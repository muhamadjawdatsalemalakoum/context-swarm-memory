# AUDIT 2026-09-05 — full candidate record

A 14-layer, 25-finder audit of the whole repository produced the candidates
below (383 after de-duplication by file, line and category). Agent-side
adversarial verification was killed twice by usage limits, so **nothing here
was auto-verified**; every item that was acted on was checked by hand against
the code or the artifact first. The twelve fix units are commits
`e952618..HEAD`, itemised in `CHANGELOG.md`; the invariant scorecard is in
`STATUS.md`.

**How to read the status column.** `refuted` = checked by hand and the finder
was wrong (reason given). `disclosed` = real, deliberately not changed, and
documented as an exception. `in a file changed by the audit` = the finding's
file was edited by one or more fix units; the specific line may or may not have
been the target, so **re-verify against HEAD before acting**. `unaddressed` = not
touched; the finder's claim is unverified and stands only as a pointer. Nothing
in the `unaddressed` set touches a published number.

| status | count |
|---|---:|
| refuted | 4 |
| disclosed | 1 |
| in a file changed by the audit — re-verify against HEAD | 281 |
| unaddressed | 97 |


## refuted (4)

| file:line | sev | category | layer | claim | note |
|---|---|---|---|---|---|
| `scripts/answer-arms.ts:129` | high | invariant | gate-scripts | renderExcerpts silently truncates each arm's rendered context at 120,000 chars with no count, and on the fact-fold paired arm that STATUS cites as lever evidence this cut 16 of 70 fold-arm contexts (up to 94,681 chars / 16 of 25 documents) versus 1 of 70 control contexts. | The 120K cap exists but the certified instrument is headtohead-arms (200K cap); the 200K cap DID fire and is fixed/disclosed under M17/M18. |
| `src/core/coverage.ts:161` | medium | invariant | core-read-path | `INTENT_SHAPE_WORDS` contains "mistake", a word that matches no CUE regex and was lifted from a PaySwift benchmark query, contradicting the set's own rule and invariant 6. | coverage.ts:62 and :153 document "mistake" as a deliberate retrospective-shape cue; generic English, not a corpus entity. |
| `docs/STATUS.md:137` | low | drift | verifier-and-charts | STATUS.md says tests/env.test.ts pins twelve flag rows; the FLAGS table has thirteen. | The FLAGS table has twelve rows (counted); the finder said thirteen. |
| `scripts/answer-arms.ts:129` | low | correctness | gate-scripts | renderExcerpts silently truncates the joined excerpts at 120,000 chars per arm with no count, so the two-process gate can grade a shorter context than the arm returned without saying so. | The 120K cap exists but the certified instrument is headtohead-arms (200K cap); the 200K cap DID fire and is fixed/disclosed under M17/M18. |

## disclosed (1)

| file:line | sev | category | layer | claim | note |
|---|---|---|---|---|---|
| `src/core/digestSelection.ts:154` | medium | invariant | core-read-path | `HIGH_SIGNAL` is a hand-written decision/business vocabulary table (decid/approv/launch/block/deadline/price/reject/deny/chose/select/migrat/version) that boosts fragment retention in the recall digest — neither corpus-derived nor structural (invariant 6). | retained as a disclosed invariant-6 exception (default-OFF measured lever); see header comment. |

## in a file changed by the audit — re-verify against HEAD (281)

| file:line | sev | category | layer | claim | note |
|---|---|---|---|---|---|
| `docs/AMB_BEAM_100K_OFFICIAL_RERUN.md:4` | high | drift | docs-top-level | The official-rerun doc still describes PR #19 as submitted and pending acceptance, with no SUPERSEDED/stale banner, directly contradicting the authoritative STATUS page. |  |
| `docs/EVIDENCE.md:169` | high | drift | docs-experiments | The Evidence column labels three sidecar-measured lever verdicts as 'gemini', including the hybrid-router row, when the backing run configs record providerName 'agent-sdk' (the Claude sidecar). |  |
| `docs/SOTA_BENCHMARK_PLAN.md:87` | high | drift | docs-top-level | The document states, in the present tense and with no SUPERSEDED banner, a public claim that CSM beats Hindsight — and EVIDENCE.md:60 still points to it as the current target ladder. |  |
| `docs/assets/beam-token-cost.svg:30` | high | drift | docs-top-level | The chart embedded in README.md:67 and AMB_BEAM_LADDER_2026_06_18.md:59 still carries verbatim the headline that CHANGELOG.md:75-89 says was corrected to 76x. |  |
| `docs/experiments/EXP-category-leadership-2026-08.md:316` | high | correctness | docs-experiments | The 1M knowledge_update improvement (official 0.229 → 0.639 at n=70) is attributed to the preference-profile lever being 'default-on now', but the profile is default OFF and the arm that produced 0.639 ran with preferenceProfile:false. |  |
| `docs/experiments/EXP-category-leadership-2026-08.md:317` | high | drift | docs-experiments | The 1M knowledge_update improvement (0.229 -> 0.639) is attributed to the preference-profile lever being 'default-on now', but the run that produced 0.639 ran profile-OFF and the flag has never been default-on in code. |  |
| `docs/experiments/EXP-router-1m-hybrid.md:64` | high | drift | docs-experiments | The +0.365 / 26W/5L answer-gate result that STATUS.md:143, EVIDENCE.md:89/169, PREFLIGHT_OFFICIAL_LADDER.md:19 and tests/env.test.ts:146 cite as the evidence for the hybrid-router default was graded through the capsule render gap and has never been re-measured, yet none of those places carries the retraction caveat. |  |
| `integrations/amb/csm_provider.py:156` | high | workaround | ops-and-integrations | retrieve() silently respawns a dead warm server with zero documents and nothing re-ingests, so every remaining query in the unit is scored against an empty context and saved as a normal (wrong) row that the ladder then counts toward tier completion. |  |
| `integrations/amb/csm_provider.py:156` | high | correctness | ops-and-integrations | retrieve() silently restarts a dead bridge server with ZERO documents and serves the rest of the unit from it; AMB records those queries as wrong ("empty context") rows, the ladder counts them toward the completion target, and --skip-ingested then skips the corrupted unit on every retry. |  |
| `integrations/amb/csm_provider.py:189` | high | correctness | amb-bridge | If the warm server process dies mid-run, `_ensure_server()` (called from every `retrieve()`) silently respawns an EMPTY server and never replays `documents.jsonl`, so every remaining query returns `no_documents_in_scope` with HTTP 200 and the upstream runner scores each one as wrong without any error. |  |
| `scripts/amb-csm-retrieve.ts:579` | high | drift | amb-bridge | The 'fold, never append' claim is false for a large share of certified rows: when no capsule exists (point queries — most knowledge_update/abstention questions), the fact registry is emitted as a standalone `csm-fact-registry` document, i.e. appended, and the on-disk certified artifacts show this happened on 25/70 (1M knowledge_update), 28/70 (500K knowledge_update), 21/70 (500K information_extraction) and 40/70 (1M abstention) rows. |  |
| `scripts/amb-csm-retrieve.ts:703` | high | invariant | amb-bridge | `resolveVirtualShardSize` hand-parses `CSM_VIRTUAL_SHARDS` with `Number.parseInt` and silently maps any unrecognised value to 0 (OFF), bypassing `src/utils/env.ts` — a direct violation of invariant 5 (every CSM_* read goes through env.ts; unrecognised value throws). |  |
| `scripts/amb-csm-retrieve.ts:704` | high | invariant | amb-bridge | `CSM_VIRTUAL_SHARDS` is hand-parsed with `Number.parseInt` and any unrecognised value (`on`, `4x`, `yes`) silently resolves to 0 = OFF, bypassing src/utils/env.ts — the exact silent-default class invariant 5 forbids; this is the only reader of the variable in the repo and it is not pinned in tests/env.test.ts. |  |
| `scripts/amb-csm-retrieve.ts:1207` | high | invariant | amb-bridge | Term-scored candidate ranking in the bridge uses bare `.sort().slice()` instead of `src/core/selection.ts:select()`, so ties at the cut are resolved silently and the degeneracy is never reported — at :1204-1212 (selectChronologicalCoverageIds, decides which events are RETURNED on the legacy path), :1342-1352 (selectTopCoverageIds), :1405-1413 (selectBroadSummaryEvidence), :1442-1457 (prioritizeTemporalEvidence), :1535-1546 (bestAnchorForTerms) and :884-889 (buildSessionDigests, default-OFF). |  |
| `src/cli/index.ts:229` | high | invariant | storage-utils-cli | An unrecognised CSM_PROVIDER value (e.g. a typo) silently runs the whole CLI on MockProvider instead of throwing, violating invariant 5 (unrecognised config value is an error, never a default). |  |
| `src/core/coverage.ts:796` | high | invariant | core-read-path | Chronicle assembly performs bare score→sort→slice cuts at eight sites with no degeneracy report, never routing through `select()` (invariant 4), on the default-ON coverage path. |  |
| `src/core/coverage.ts:1181` | high | invariant | docs-top-level | OUT OF LAYER (code pointer): CSM_COVERAGE_RECALL_TOKENS is read from process.env and hand-parsed by a local parsePositiveInt that silently falls back on garbage, violating invariant 5 (every CSM_* read goes through src/utils/env.ts and throws on unrecognised values). |  |
| `src/core/coverage.ts:1264` | high | invariant | core-read-path | `resolveCoverageStarvationFloor` hand-parses `CSM_COVERAGE_STARVATION_FLOOR` and silently reverts to 4 on garbage or negative values (invariant 5). |  |
| `src/core/coverage.ts:1272` | high | invariant | core-read-path | The private `parsePositiveInt` silently falls back on unparseable or non-positive input and still backs two live `CSM_COVERAGE_*` reads (invariant 5), on the default-ON coverage path. |  |
| `src/core/prompts.ts:114` | high | invariant | core-write-and-contracts | The live recall prompt embeds a PaySwift benchmark answer entity ('ChairSync') and the exact alias mapping ('dental-SaaS vendor' -> ChairSync) that PaySwift MCQ q11 tests, and this text is sent on every recall call for every corpus. |  |
| `src/core/retrievalUnit.ts:167` | high | invariant | core-read-path | `resolveUnitSize` hand-parses `CSM_RETRIEVAL_UNITS` and returns 0 (lever OFF) for any unparseable or negative value (invariant 5). |  |
| `src/core/tokenBudget.ts:44` | high | invariant | core-read-path | `resolveRecallBudget` hand-parses `CSM_RECALL_BUDGET` and silently returns the default on garbage, zero, negative or fractional input, bypassing `src/utils/env.ts` (invariant 5). |  |
| `src/core/tokenBudget.ts:45` | high | invariant | eval-harness | CSM_RECALL_BUDGET is hand-parsed and silently falls back to the default on any unparseable value, violating invariant 5 (unrecognised CSM_* value must THROW); the read is live at src/core/ask.ts:120. |  |
| `src/core/tokenBudget.ts:70` | high | invariant | core-read-path | `resolveShardCount` hand-parses `CSM_MAX_PROBE_SHARDS` / `CSM_MAX_RECALL_SHARDS` with a silent fallback on NaN/<=0 and a silent floor on fractions (invariant 5). |  |
| `src/eval/baselines/csm.ts:276` | high | invariant | eval-csm-baseline | resolveShardExpandK/resolveShardExpandMax hand-parse CSM_SHARD_EXPAND_K/_MAX with Number.parseInt and silently fall back to 3/16 on an unrecognised value, violating invariant 5 (unrecognised CSM_* value must THROW via src/utils/env.ts). |  |
| `src/eval/baselines/csm.ts:331` | high | invariant | eval-csm-baseline | buildEntityBridgeGroups ranks scored same-shard candidates with a bare .sort().slice(0, rankedLimit) instead of src/core/selection.ts:select(), so pervasive ties in the integer term-hit score are resolved silently by id-descending order and never reported (invariant 4). The lever is default-ON (CSM_ENTITY_BRIDGE_K=6). |  |
| `src/eval/baselines/csm.ts:374` | high | invariant | eval-csm-baseline | buildLocalLexicalBridgeGroups has the same bare .sort().slice(0, rankedLimit) on tied integer bridgeScore values with no degeneracy report (invariant 4). Lever is default-OFF (CSM_LEXICAL_BRIDGE_K=0) but is an A/B arm in scripts/run-beam-slice.ts:142. |  |
| `src/eval/baselines/csm.ts:389` | high | invariant | eval-csm-baseline | resolveLexicalBridgeK/resolveLexicalBridgeMax hand-parse CSM_LEXICAL_BRIDGE_K/_MAX and silently fall back to 0/20 on garbage (invariant 5). |  |
| `src/eval/baselines/csm.ts:403` | high | invariant | eval-csm-baseline | resolveEntityBridgeK/resolveEntityBridgeMax hand-parse CSM_ENTITY_BRIDGE_K/_MAX and silently fall back to 6/24 on garbage; this lever is DEFAULT-ON in the retrieval path, so a mis-typed 'off' runs the treatment (invariant 5). |  |
| `src/providers/GeminiProvider.ts:47` | high | invariant | providers | resolveGeminiCacheMode hand-parses CSM_GEMINI_CACHE and on an unrecognised value logs once and returns 'off' — the silent-default pattern invariant 5 forbids — in a file that already imports envEnum (line 11) and uses it for CSM_GEMINI_THINKING; the pinning test is named '…rejects_unknown' but asserts acceptance. |  |
| `src/providers/LlmProvider.ts:120` | high | invariant | providers | selectProviderName hand-parses CSM_PROVIDER and silently maps any unrecognised value (e.g. a typo like 'gemni') to URL auto-detect and then to 'mock' instead of throwing, violating invariant 5 (unrecognised CSM_* value is an error, never a default); createProvider adds a second silent layer with `case "mock": default:`. |  |
| `tests/coverageAssembler.test.ts:339` | high | invariant | tests | The test pins silent defaulting for CSM_COVERAGE_RECALL_TOKENS ('garbage' -> default) and, at line 369, for CSM_COVERAGE_STARVATION_FLOOR ('nope' -> 4); both resolvers bypass src/utils/env.ts. |  |
| `tests/coverageAssembler.test.ts:340` | high | invariant | tests | Test pins that CSM_COVERAGE_RECALL_TOKENS='garbage' silently resolves to DEFAULT_COVERAGE_RECALL_TOKENS (invariant 5). |  |
| `tests/coverageAssembler.test.ts:366` | high | invariant | tests | Test pins that CSM_COVERAGE_STARVATION_FLOOR='nope' silently resolves to 4 (invariant 5). |  |
| `tests/digestSelection.test.ts:124` | high | invariant | tests | The test pins a silent default for CSM_RECALL_BUDGET: a garbage value resolves to the fallback instead of throwing, which is the exact behaviour invariant 5 (and this suite's own env.test.ts header) forbids. |  |
| `tests/embeddingFloor.test.ts:177` | high | invariant | tests | The file pins silent defaulting for CSM_SHARD_EXPAND_K/MAX while, 80 lines earlier, calling that exact shape 'the exact shape the env.ts invariant exists to forbid' for CSM_EMBED_FLOOR_K. |  |
| `tests/geminiCaching.test.ts:144` | high | invariant | tests | A test titled 'defaults_off_and_rejects_unknown' asserts that an unknown CSM_GEMINI_CACHE value resolves to 'off' — it pins a silent default, not a rejection. |  |
| `tests/retrievalUnit.test.ts:122` | high | invariant | tests | The test pins silent defaulting for CSM_RETRIEVAL_UNITS: 'nonsense' and '-4' resolve to 0 (off) instead of throwing. |  |
| `CHANGELOG.md:94` | medium | drift | docs-top-level | The changelog states the 'CSM's accounting is the complete one' comparison 'is removed everywhere', but it survives in four docs. |  |
| `CLAUDE.md:94` | medium | drift | docs-top-level | CLAUDE.md and CONTRIBUTING.md say Node 20 is supported; package.json requires >=22 and CI/README say 22. |  |
| `README.md:61` | medium | drift | amb-bridge | README and STATUS describe the official instrument as the 'unmodified public AMB runner … their judge path, a 3-file CSM provider and nothing else', but `npm run amb:patch` also edits upstream `src/memory_bench/llm/gemini.py` — the LLM client used by AMB's answer and judge calls — adding a client timeout and widening the retry guard to timeout/transport errors. |  |
| `README.md:67` | medium | drift | docs-top-level | The README chart alt text and the embedded SVG still say the haystack grows ~100×, which CHANGELOG.md:86-89 says was corrected to 76× in 'chart caption, chart alt text and all meta/JSON-LD'. |  |
| `README.md:145` | medium | drift | docs-top-level | The README defaults table omits the 'coverage mode + chronicle (`CSM_COVERAGE`) ON' and 'legacy vocab/intent OFF' rows that STATUS.md:148-149 lists, although CHANGELOG.md:54-56 claims both defaults tables were fixed. |  |
| `README.md:152` | medium | drift | docs-top-level | The README defaults table omits coverage mode ON and legacy vocab/intent OFF even though CHANGELOG.md:54-56 says both tables now list them. |  |
| `README.md:266` | medium | drift | docs-top-level | 'How it works' describes hybrid embedding routing and zero-token ingest as CSM's behaviour, but the Quickstart CLI has no hybrid-router wiring and the README's own cost section says the default fold reads the whole corpus with an LLM at ingest. |  |
| `docs/AMB_BEAM_LADDER_2026_06_18.md:8` | medium | drift | docs-top-level | The ladder doc STATUS declares 'stale twice over' has no stale/superseded banner and still presents PR #19 without noting it was closed. |  |
| `docs/ARCHITECTURE.md:109` | medium | drift | docs-top-level | Present-tense description of four eval baselines that were removed; src/eval/baselines/ contains only csm.ts and types.ts. |  |
| `docs/EVIDENCE.md:41` | medium | drift | docs-top-level | Two rebuild commands cite scripts that do not exist in scripts/. |  |
| `docs/EVIDENCE.md:109` | medium | drift | verifier-and-charts | EVIDENCE.md says `npm run verify:published` pins both the answer-visible and the CSM-internal token component per ladder tier, but assertBeamLadder pins only `context_tokens`; the internal figures (8.8/9.6/9.9/3.4K) behind the README all-in headline 35.8/36.2/38.1/35.9K are not read, hashed, or recomputed for 100k-v2/500k/1m/10m. |  |
| `docs/EVIDENCE.md:186` | medium | drift | docs-top-level | The lever ledger says CSM_AMB_OBSERVE_MEMORY was 'never validly measured', while WRITE_TIME_MEMORY_2026_07.md:81 publishes a measured +0.2224 for it and the committed artifact is official-runner output, which the render-gap post-mortem says is unaffected. |  |
| `docs/EVIDENCE.md:209` | medium | drift | verifier-and-charts | The 'Known limitations' section still describes PR #19 as pending, contradicting STATUS.md:76-78 and EVIDENCE.md's own preamble (line 7). |  |
| `docs/EVIDENCE.md:212` | medium | drift | docs-top-level | The 'CSM fully accounted vs Hindsight discloses no internal cost' comparison that CHANGELOG.md:94 says 'is removed everywhere' survives here and in three other docs. |  |
| `docs/GEMINI.md:127` | medium | drift | docs-top-level | The documented cost-smoke command names a `rag` system that no longer exists; the CLI silently drops it and runs csm only. |  |
| `docs/GEMINI.md:193` | medium | dead-code | providers | The 'SOTA sidecar mode' section (and .env.example:66-70) tells users to set `CSM_GEMINI_REASONING_EFFORT` and run `npm run proxy:start`; no code reads that variable, no such npm script exists, and the Python sidecars it configures are not in the repo. |  |
| `docs/GEMINI.md:194` | medium | drift | docs-top-level | The 'SOTA sidecar mode' section documents an npm script and Python sidecars that no longer exist. |  |
| `docs/PREFLIGHT_OFFICIAL_LADDER.md:96` | medium | drift | docs-top-level | The launch recipe says internals run on gemini-3.5-flash and that no behavioural CSM_* flag is set, but the tier script exports a non-default probe model that PERF_BREAKDOWN says is an opt-in not yet validated as the default. |  |
| `docs/REPRODUCING.md:70` | medium | drift | docs-top-level | Four npm scripts documented as runnable do not exist in package.json, and the referenced services/ directory does not exist. |  |
| `docs/STATUS.md:39` | medium | drift | gate-scripts | The exclusion caveat under the certified table attributes 7 excluded pairs to the 500K knowledge_update second-reader row, but only 3 of those 7 are knowledge_update pairs; 70 − 7 ≠ 67. |  |
| `docs/STATUS.md:56` | medium | drift | docs-top-level | STATUS juxtaposes official-ladder category deltas with free-instrument deltas as a before/after on 'the same 1M queries', violating its own rule (line 213-215) that numbers from different instruments are never pooled and only within-instrument deltas are compared. |  |
| `docs/STATUS.md:141` | medium | drift | eval-csm-baseline | The 'Current defaults — the certified configuration' table omits four default-ON retrieval-order augmentations that csm.ts applies on every certified query: the RAG floor (always on), embedding recall floor (CSM_EMBED_FLOOR_K=10), shard-local dense expansion (CSM_SHARD_EXPAND_K=3) and entity bridge (CSM_ENTITY_BRIDGE_K=6). |  |
| `docs/STATUS.md:149` | medium | drift | eval-harness | The authoritative defaults table attributes the measured-negative 'coverage reranker' to `CSM_HYBRID_RERANK`, but that flag gates src/eval/rerank.ts, which has no production caller; the lever that was actually measured (4W/13L) is `CSM_AMB_COVERAGE_RERANK` in the bridge. |  |
| `docs/STATUS.md:149` | medium | correctness | docs-experiments | STATUS attributes the rejected coverage-reranker verdict to `CSM_HYBRID_RERANK`, but the lever measured and rejected in the experiments layer is `CSM_AMB_COVERAGE_RERANK`; `CSM_HYBRID_RERANK` is the unrelated cross-encoder reranker. |  |
| `docs/STATUS.md:210` | medium | drift | docs-experiments | STATUS presents the free judge as 'calibrated ... vs the official judge' without noting that the judge it was calibrated against (gemini-2.5-flash-lite, rubric fraction + Kendall tau-b) was replaced upstream after PR #20, which STATUS itself records two sections earlier. |  |
| `docs/WRITE_TIME_MEMORY_2026_07.md:187` | medium | drift | docs-top-level | References a harness script that does not exist, plus a stale test count and stale PR #19 wording, in a doc README links as current. |  |
| `docs/experiments/EXP-T2-router.md:452` | medium | drift | docs-experiments | The live-gate verdict says CSM_ROUTER_HYBRID stays default-off with no forward pointer, while the shipped default is ON. |  |
| `docs/experiments/EXP-capsule-render-gap.md:80` | medium | drift | docs-experiments | The retraction lists CSM_AMB_OBSERVE_MEMORY among levers 'measured as content no-ops', and EVIDENCE.md marks it 'never validly measured', but an official-config (Gemini, real bridge) measurement of +0.2224 exists and is still quoted as live in EXP-retrieval-units.md — three docs disagree about the same lever. |  |
| `docs/experiments/EXP-category-leadership-2026-08.md:43` | medium | drift | docs-experiments | The document uses three different sign conventions for delta, so the same effect appears as +0.096 and −0.0964 in different tables. |  |
| `docs/experiments/EXP-category-leadership-2026-08.md:82` | medium | drift | docs-experiments | The 'today's config' scoreboard carries no run id and its 500K cells disagree with the run-id-labelled table above it and with two other baselines used later, so information_extraction at 500K has three different 'baseline' deficits in one document. |  |
| `docs/experiments/EXP-category-leadership-2026-08.md:264` | medium | drift | docs-experiments | Two arms are left as 'running' in the doc although their results are on disk and are already relied on by STATUS/EVIDENCE for default decisions (lean revert; fact-fold default flip). |  |
| `docs/experiments/EXP-category-leadership-2026-08.md:316` | medium | drift | docs-top-level | OUT-OF-LAYER pointer: the source doc for the README/STATUS 'today's defaults' table attributes the knowledge_update improvement to the preference-profile lever being 'default-on now', but the profile default is OFF per STATUS.md:147, EVIDENCE.md:171 and tests/env.test.ts:182-184. |  |
| `docs/experiments/EXP-category-leadership-2026-08.md:340` | medium | correctness | docs-experiments | The repeats=3 table reports the 500K deltas with the sign inverted relative to the FINAL table and the cross-reader table (−0.0964/−0.0750/−0.0857 vs +0.096/+0.075/+0.086 and +0.099/+0.075/+0.087) while the prose says CSM is 'directionally ahead'; the doc also declares positive = Hindsight ahead at line 43 and then uses positive = CSM ahead from line 90 onward. |  |
| `docs/experiments/EXP-category-leadership-2026-08.md:535` | medium | drift | docs-experiments | The STANDING SCOREBOARD claims CSM is ahead in >=2 categories at every tier by pooling official-Gemini-ladder leads (100K: 7, 10M: 4) with free-instrument leads, which the same document forbids and STATUS.md contradicts. |  |
| `docs/experiments/EXP-category-leadership-2026-08.md:535` | medium | correctness | docs-experiments | The STANDING SCOREBOARD claims CSM is 'ahead in ≥2 categories at every tier' by pooling official-ladder leads (100K: 7, 10M: 4) with free-instrument leads — exactly the pooling the same doc calls an instrument error 240 lines earlier, and a claim STATUS.md says is not met; the 10M leads additionally come from a tier STATUS says measured a barely-loaded corpus. |  |
| `docs/experiments/EXP-router-1m-hybrid.md:3` | medium | drift | docs-experiments | The status header and the 'What this does NOT yet establish' section say the lever is not answer-gated and 'Nothing is enabled by default until it does', contradicting the doc's own later sections and the shipped default. |  |
| `docs/experiments/EXP-system-audit-2026-07.md:356` | medium | drift | docs-experiments | The audit asserts arm H's score and its sign flip against Hindsight 'stand unchanged', which the render-gap retraction written the next day contradicts, with no forward note here. |  |
| `docs/experiments/EXP-system-audit-2026-07.md:393` | medium | drift | core-read-path | The audit record claims every integer env read routes through env.ts; seven `CSM_*` integer read sites in the core read path do not. |  |
| `docs/experiments/EXP-token-efficiency-2026-08.md:38` | medium | drift | docs-experiments | The doc says every shipped lever is default-off, calls lean K=16 the shipping point, and leaves the composition arm 'running', but CSM_PROBE_BATCH is default ON for hosted providers, lean K=16 was flipped and reverted, and r1mM-shipstack-v1 finished the same day. |  |
| `docs/experiments/EXP-token-efficiency-2026-08.md:68` | medium | drift | docs-experiments | The doc declares lean K=16 'the shipping point', projects CSM's answer context 'below Hindsight's 17.9K' on that basis, and says the composition arm is 'running' with defaults to flip on its verdict — but lean K=16 was flipped ON 2026-08-01 and REVERTED 2026-08-02 for a 500K information_extraction regression, and this doc records neither the flip, the verdict, nor the revert. |  |
| `docs/experiments/EXP-virtual-shards-system.md:3` | medium | drift | docs-experiments | Several experiments docs state as their headline verdict a default that has since flipped, with no forward pointer: CSM_SHARD_DESCRIPTORS 'stays default-off' (it is ON), CSM_ROUTER_HYBRID 'stays default-off' (ON), 'the flags remain default-off' (ON), and the token campaign's 'all default-off' (CSM_PROBE_BATCH is ON for hosted providers). |  |
| `integrations/amb/README.md:47` | medium | drift | ops-and-integrations | The bridge's entry-point README still presents the May 2026 pre-#20 comparison (0.757573 vs 0.733658) as the current result and carries a 2026-06-09 status banner, with no pointer to STATUS.md's ruling that these numbers are not comparable to anything current and that officialization ended with PR #19 closed. |  |
| `integrations/amb/csm_provider.py:118` | medium | correctness | amb-bridge | The resume path replays the entire `documents.jsonl` in ONE POST /ingest, but the server caps request bodies at 256 MiB; the completed 10M store on disk is 329,246,098 bytes, so a `-SkipIngested` resume of a fully-ingested 10M tier is rejected with HTTP 400 and the provider raises RuntimeError. |  |
| `integrations/amb/csm_provider.py:226` | medium | workaround | ops-and-integrations | The startup deadline is only checked between stdout lines; `readline()` blocks, so a child that stays alive but prints nothing hangs initialize() indefinitely and CSM_AMB_SERVER_STARTUP_TIMEOUT_SEC never fires. |  |
| `integrations/amb/csm_provider.py:309` | medium | drift | amb-bridge | The telemetry comment claims `inputTokens`/`outputTokens` 'cover every LLM call the bridge made', but with the default-ON fact fold the O(corpus) registry build is never attributed: `loadOrBuildFactRegistry` drops the build's `inputTokens`, the server discards even the returned `outputTokens`, and `factBuildCost` on the fold path is always null. |  |
| `scripts/amb-csm-retrieve.ts:538` | medium | drift | amb-bridge | Code comments assert a slot-displacement mechanism ('an added document evicts evidence that was working', 'measured four separate times') that this code cannot produce — the capsule is prepended OUTSIDE the k-slice — and that STATUS.md has retracted. |  |
| `scripts/amb-csm-retrieve.ts:541` | medium | drift | amb-bridge | Code comments state that displacement (an added document evicting evidence within a fixed RETURN_K) has been 'measured four separate times' and that the capsule 'costs exactly one of the ~24 returned slots', but docs/STATUS.md:173 marks the fold-vs-append displacement result RETRACTED as a harness artifact, and the bridge itself never caps at k: it returns k+1 documents whenever a capsule exists. |  |
| `scripts/amb-csm-retrieve.ts:631` | medium | correctness | amb-bridge | When the fold registry build fails, the query is silently graded WITHOUT the certified lever and the payload carries no marker — `factBuildCost` is null on the fold path either way and `evidenceCapsule` is false for any point query — so fold-on and fold-degraded rows are indistinguishable in raw_response/telemetry; the certified artifacts contain such rows (g1m-ku70-factfold-v1: 20_knowledge_update_0 and _1; g500k-factfold-v1: 23_knowledge_update_0) that STATUS.md reports as full n=70 arms. |  |
| `scripts/amb-csm-retrieve.ts:1204` | medium | invariant | amb-bridge | The bridge's legacy return/capsule path ranks term-scored candidates with bare `.sort().slice()` at six sites instead of `src/core/selection.ts:select()`, so it never reports degeneracy (invariant 4); the only import from selection.ts is `dedupeInOrder`. |  |
| `scripts/amb-csm-retrieve.ts:1786` | medium | test-gap | tests | The F9/F10 corpus-vocabulary tables ('security' -> flask-wtf/redis/lockout; LEGACY_HIGH_SIGNAL_TERMS) are still compiled into the retrieval bridge behind CSM_AMB_LEGACY_VOCAB, and no test references that flag or pins that the default bridge path is inert — while CLAUDE.md says the tables 'were removed'. |  |
| `scripts/amb-csm-retrieve.ts:1903` | medium | test-gap | tests | The F9/F10 corpus-vocabulary tables ('security' → flask-wtf, redis, lockout …; LEGACY_HIGH_SIGNAL_TERMS) are still compiled into the retrieval bridge behind CSM_AMB_LEGACY_VOCAB, and no test references the flag or pins that the default path is inert — while CLAUDE.md states the tables 'were removed'. |  |
| `scripts/amb-csm-server.ts:372` | medium | correctness | amb-bridge | A fold-registry build failure degrades the query to no-registry with no per-row marker in the payload: `raw_response.factBuildCost` is null on the fold path either way and `evidenceCapsule` is true whenever any capsule exists, so a run can mix fold-ON and fold-OFF rows indistinguishably — while the observation build failure on the same server propagates as HTTP 500 ('loud failure preserved'). |  |
| `scripts/amb-csm-server.ts:471` | medium | correctness | amb-bridge | The write-time disk caches (fact registry = certified default-ON lever, preference profile) are keyed by `split\|userId\|model\|promptVersion` where split defaults to the literal "amb" and userId is the BEAM unit number (1..N in every tier); the server cannot detect cross-tier reuse, so any launch that does not set CSM_AMB_SPLIT serves tier-1 registries to same-numbered tier-2+ units. The only defence is `scripts/run-beam-tier.ps1:61`; docs/MAC_HANDOVER.md:73-80 and docs/AMB_OFFICIALIZATION_STATUS.md:137-145 both describe running `omb` directly without it. |  |
| `scripts/amb-fact-registry.ts:83` | medium | correctness | amb-bridge | The write-time disk-cache key contains no content digest — only split\|userId\|model\|promptVersion with the server defaulting split to the literal "amb" — so any launch that does not set CSM_AMB_SPLIT (the documented `uv run omb run ...` recipes) silently serves tier-1 registries/profiles to same-numbered units of every other tier, and the server cannot detect it. |  |
| `scripts/amb-fact-registry.ts:109` | medium | workaround | amb-bridge | `loadOrBuildFactRegistry` treats ANY existing cache file as a valid registry, including an empty one, and the same function persists empty build output; two zero-byte registries already sit in the real cache directory, and such a file makes the fold silently absent for that key forever while the server logs 'registry from disk cache'. |  |
| `scripts/amb-fact-registry.ts:150` | medium | correctness | amb-bridge | An EMPTY build result is written to the disk cache and a 0-byte cache file is treated as a hit, so once a build returns empty text the fold is permanently and silently OFF for that split\|user\|model key (`factActive` requires length > 0); 0-byte entries already exist on disk. |  |
| `scripts/free-ladder-report.mjs:83` | medium | correctness | gate-scripts | The report's 'certified' verdict drops the CI condition and the n>=2 guard that the head-to-head applies, so it can certify where headtohead-arms says tie (including at n=1, where se=0 makes any nonzero delta 'certified'). |  |
| `scripts/free-ladder-tier.sh:44` | medium | correctness | gate-scripts | The autopilot does not check headtohead-arms' exit status (unlike the emit-contexts step), so on failure it runs the report on whatever stale headtohead JSON already exists for that tier label. |  |
| `scripts/free-ladder-tier.sh:44` | medium | workaround | gate-scripts | The head-to-head step has no exit-status check (unlike emit-contexts at L38-41) and the script runs without -e, so on failure the report step summarises whatever stale `<TIER>-free.json` already exists. |  |
| `scripts/headtohead-arms.ts:136` | medium | correctness | gate-scripts | The reader's context is sliced at 200,000 chars per arm silently and asymmetrically; CSM contexts exceeded it on disk while Hindsight's never do, and free-ladder-report's token count then claims byte-identity with what the reader saw. |  |
| `scripts/headtohead-arms.ts:136` | medium | invariant | gate-scripts | The CSM arm's context is silently truncated at 200,000 chars per arm with no count, and this actually fired inside a certified arm: 46% of one 500K fact-fold context was dropped before the reader saw it. |  |
| `scripts/headtohead-arms.ts:246` | medium | correctness | gate-scripts | `excluded` merges no-rubric, unparsable-judge and sidecar/throttle errors and never records which query or which ARM failed, so the arm-correlation of exclusions behind STATUS's n=67/69 second-reader rows cannot be checked from the artifact. |  |
| `scripts/headtohead-arms.ts:298` | medium | correctness | gate-scripts | The result artifact is keyed only by the free-text --tier label and records neither the --csm/--hindsight paths, repeats, --per-category, nor any input hash, so on-disk suffixes are the sole provenance and free-ladder-tier.sh overwrites the primary reader's file when a second reader is run. |  |
| `scripts/headtohead-arms.ts:302` | medium | drift | gate-scripts | The head-to-head output JSON carries no provenance of what was compared — no --csm/--hindsight paths, no artifact hashes, no --per-category or --repeats — and is keyed only by the free-text --tier label, then overwritten. |  |
| `scripts/headtohead-arms.ts:302` | medium | invariant | gate-scripts | The head-to-head output JSON records reader/judge/n/excluded/results only — not `repeats`, `--per-category`, the `--csm`/`--hindsight` paths, artifact hashes, thinking level or sidecar URL — so a file cannot say how it was produced. |  |
| `scripts/measure-hindsight-10m-span.mjs:39` | medium | correctness | gate-scripts | The denominator regex matches only dated turn markers (100 per unit), so 'unit's last turn' is the last DATED turn and the published STATUS L93-94 ranges are overstated: recomputed span is 63.5%–88.8% (not 64.3%–89.7%) and density 11.5%–15.8% (not 11.6%–15.9%). |  |
| `scripts/run-beam-ladder.ps1:16` | medium | drift | ops-and-integrations | The ladder's own .EXAMPLE (and PREFLIGHT's one-command launch recipe) invoke `pwsh` without -Tag; -Tag is mandatory, pwsh is not installed on this host, and if pwsh were used the single-instance guard and watchdog only look for powershell.exe. |  |
| `scripts/run-beam-ladder.ps1:56` | medium | correctness | ops-and-integrations | The single-instance guard (and the watchdog liveness check) only look at processes named powershell.exe, but the documented launcher for the ladder is pwsh (pwsh.exe); a pwsh-launched ladder is invisible to both, and on this machine pwsh is not installed at all so the documented one-command launch fails outright. |  |
| `scripts/run-beam-ladder.ps1:57` | medium | correctness | ops-and-integrations | The guard force-kills every uv.exe/python.exe whose command line contains the bare substring 'omb', which matches unrelated processes (any path or argument containing 'comb', 'zombie', 'Bombay', ...). |  |
| `scripts/run-beam-slice.ts:288` | medium | correctness | gate-scripts | config.json is rewritten unconditionally on every invocation, including resumes, so a payloads.jsonl assembled across invocations under different env/commit carries only the LAST invocation's gitSha/envEcho/resolvedLevers — the F7/F11 failure the manifest exists to prevent. |  |
| `scripts/run-beam-slice.ts:288` | medium | invariant | gate-scripts | config.json is rewritten on every invocation, including resumes, so a resumed run's manifest (gitSha, envEcho, resolvedLevers, startedAtIso) describes only the last invocation while payloads.jsonl may hold rows produced under a different commit or env. |  |
| `scripts/run-beam-tier.ps1:4` | medium | drift | ops-and-integrations | The docstring asserts the script runs 'the frozen CSM pipeline (src/ identical to commit 599dfc0)' and that its env block 'matches ... RUN_MANIFEST.md exactly'; neither is true today. |  |
| `scripts/run-beam-tier.ps1:13` | medium | drift | ops-and-integrations | The docstring claims the answer/judge models are 'the same path as the accepted Hindsight artifact', which STATUS says is no longer a valid comparison; the script pins no AMB runner commit even though PREFLIGHT says to, and the local AMB checkout it runs against is pre-PR-#20. |  |
| `scripts/run-beam-tier.ps1:73` | medium | drift | ops-and-integrations | STATUS says the official run sets 'no behavioural CSM_* flag' and PREFLIGHT lists internals as gemini-3.5-flash only, but the tier script routes the probe stage to a different model (gemini-2.5-flash-lite) via CSM_PROBE_MODEL. |  |
| `scripts/run-beam-tier.ps1:98` | medium | correctness | ops-and-integrations | Under `$ErrorActionPreference = 'Stop'` the guard's Write-Error is a terminating exception, so `exit 2` is unreachable and the ladder treats the intentional abort as a retryable crash. |  |
| `scripts/run-beam-watchdog.ps1:47` | medium | dead-code | ops-and-integrations | The watchdog relaunches the ladder without the now-mandatory -Tag, so a relaunch would block on PowerShell's parameter prompt in a hidden window; the hung process matches the watchdog's own liveness filter, so every later tick logs 'ladder alive' forever. The script is unregistered, unreferenced by any doc, and its tier list is hardcoded to the completed June dirs. |  |
| `scripts/run-beam-watchdog.ps1:47` | medium | correctness | ops-and-integrations | The watchdog relaunches the ladder without the now-mandatory -Tag, so the relaunched PowerShell blocks forever on the parameter prompt in a hidden window, and step 1 then reports "ladder alive" indefinitely — the recovery mechanism deadlocks itself. |  |
| `scripts/verify-no-leakage.ts:108` | medium | test-gap | verifier-and-charts | The leakage scan targets four filler files that do not exist in the repo (and were never committed), so every public checkout prints 'Clean' after scanning zero events, yet four docs present it as a verification step. |  |
| `scripts/verify-no-leakage.ts:148` | medium | correctness | verifier-and-charts | The leakage scan declares 'Clean' and exits 0 after scanning zero events; on every public checkout it is a vacuous pass because the files it reads are gitignored and were never committed. |  |
| `src/cli/index.ts:155` | medium | correctness | storage-utils-cli | Shard IDs supplied via --id (and --shard/--snapshot/<shardId> elsewhere) are used raw as filesystem path segments with no validation; an empty id corrupts the shards directory layout and a traversal id writes outside data/. |  |
| `src/cli/index.ts:440` | medium | correctness | storage-utils-cli | `--action` and `--memory-type` are cast with `as` and never validated, so a mistyped action produces a self-contradictory dry-run (`would_mutate=true` + 'Would be a no-op') and an apply that silently does nothing, both exiting 0. |  |
| `src/cli/index.ts:581` | medium | correctness | storage-utils-cli | parseSizesFlag silently drops any --corpus-sizes / --model-contexts element it cannot parse, so a typo runs a smaller sweep than requested with no error. |  |
| `src/cli/index.ts:616` | medium | correctness | storage-utils-cli | --trials and --seed are parsed with Number.parseInt and no NaN check; a non-numeric --trials runs zero cells and records `"trials": null` in config.json, and a non-numeric --seed silently becomes seed 0 while config.json records null. |  |
| `src/cli/index.ts:618` | medium | drift | storage-utils-cli | cmdBenchRun hand-rolls its own model-resolution chain instead of using resolveProviderModel/resolveStageModels, so for CSM_PROVIDER=agent-sdk it ignores CSM_AGENT_MODEL and forwards the Ollama id `gemma4:31b` to the Claude sidecar — the exact cross-provider model leak LlmProvider.ts documents as having 'bitten three times'. |  |
| `src/cli/index.ts:620` | medium | drift | storage-utils-cli | cmdBenchRun (lines 618-623) and `provider ping` (line 493) hand-roll the model-id chain instead of calling resolveProviderModel(), contradicting the primitive's own contract at src/providers/LlmProvider.ts:174-176 ("Every site that needs a model for the active provider calls this") and reproducing the two cross-provider failures that comment documents. |  |
| `src/core/ask.ts:400` | medium | invariant | core-read-path | On the default lexical fallback and the recall-selection path the `select()` degeneracy report is computed and then discarded: `selectCandidates` returns only `.selected` (router.ts:197), the recall cut uses only `recallSelection.selected`, and neither `AskRunResult` nor `QueryRunRecord` (types.ts:191, 249) carries a discriminated/degenerateReason field, so the operator is never told. |  |
| `src/core/commit.ts:70` | medium | correctness | core-write-and-contracts | appendEventAndSnapshot performs four non-atomic writes; a crash after writeSnapshot but before saveManifest leaves an orphan S00N that makes every later append to that shard throw, with no documented recovery. |  |
| `src/core/commit.ts:255` | medium | correctness | core-write-and-contracts | dryRunCommit returns wouldMutate:true for split, merge, and any unrecognised action string while its own description says the action is a no-op and chronicleType is 'none'; applyCommitDecision then returns applied:false, so the dry-run contradicts the apply. |  |
| `src/core/commit.ts:256` | medium | correctness | core-write-and-contracts | dryRunCommit reports wouldMutate: true for split/merge (and for any unrecognised action string) while its own description says the action would be a no-op; applyCommitDecision then returns applied: false. |  |
| `src/core/commit.ts:275` | medium | invariant | core-write-and-contracts | applyCommitDecision never reads decision.requiresUserConfirmation; a write/update/freeze decision with requiresUserConfirmation=true is applied unconditionally. |  |
| `src/core/coverage.ts:550` | medium | correctness | core-read-path | `assembleChronicle` keys `indexById` (last-wins), `byEventId` (first-wins, line 504), `picked` (line 771) and the token-cap `dropped` set (line 874) by event ID alone across ALL snapshots, but durable-store event IDs are per-shard sequences (`commit.ts:48` → `e_0001`, `e_0002` in every shard), so on `csm ask` with ≥2 probed shards Phase A/B score one shard's event with another shard's token set and collapse distinct events. |  |
| `src/core/digestSelection.ts:118` | medium | invariant | core-read-path | `orderCandidates` in `reorderBySalience` mode hand-rolls score→sort followed by a token-budget cut (lines 85-99) with no degeneracy report, even though selection.ts:12 lists this function as one of the four sites unified under `select()` (invariant 4). |  |
| `src/core/recall.ts:87` | medium | invariant | core-read-path | `RecallResult.shardId`/`snapshotId` are taken from the model's echoed `shard_id`/`snapshot_id` rather than from the snapshot the call was made against, and are never compared to it (invariant 7: citations must be correct). |  |
| `src/core/routerEmbed.ts:259` | medium | workaround | core-read-path | The hybrid router's "counted, not silent" fallback is silent in practice: `hybridRouterStats()` has no consumer anywhere outside tests, so no runner, manifest or report can see that a query fell back to the degenerate lexical ranking. |  |
| `src/eval/baselines/csm.ts:438` | medium | drift | eval-csm-baseline | resolveShardDescriptors' docblock states 'Default OFF' while the body two lines later says 'DEFAULT ON since 2026-08-01' and returns fallback: true. |  |
| `src/eval/baselines/csm.ts:463` | medium | drift | eval-csm-baseline | resolveRouterHybrid's docblock says 'Default OFF on mainline … the default flips after the T3 BEAM-slice recall@k A/B confirms it', while the body says 'DEFAULT ON since 2026-08-01' and returns fallback: true. |  |
| `src/eval/baselines/csm.ts:682` | medium | correctness | eval-csm-baseline | organizeMemoryScaled/organizeFactsScaled/organizePreferencesScaled report map-stage latency as max(chunk latencies) + reduce, which equals wall-clock only when chunks ≤ mapConcurrency; at the 10M tier (~19 chunks of 600K at concurrency 4) it under-reports ingest latency by roughly ceil(N/4)×. |  |
| `src/eval/baselines/csm.ts:771` | medium | drift | eval-csm-baseline | organizePreferencesScaled's docblock says the preference profile is 'ALWAYS-ON rather than gated' and 'spends one of the ~24 returned slots on every query'; the profile is DEFAULT OFF and is FOLDED into the capsule, not appended as a slot. |  |
| `src/eval/baselines/csm.ts:1155` | medium | correctness | eval-csm-baseline | When the answer model gives an answer but no CITATIONS line, citedEventIds is overwritten with every packed event id, and the scorer computes citation precision/recall from that list — so published citation F1 includes credit the model never asserted, and no results doc discloses the substitution. |  |
| `src/eval/baselines/csm.ts:1155` | medium | drift | eval-csm-baseline | When the model answers but emits no citations, citedEventIds is silently replaced with ALL packed (or all retrieved) event ids; the scorer then computes citation precision/recall from that substituted set, no telemetry records that the substitution fired, and the published citation figures do not disclose it. |  |
| `src/eval/baselines/csm.ts:2109` | medium | correctness | eval-csm-baseline | extractBridgeTerms keeps any short (<4-char) token whose first character is upper-case and any 4+-char token regardless of stop-listing, so sentence-initial function words ('How', 'Who', 'The', 'Can', 'Did') and BEAM header boilerplate ('turn', 'context', 'user') become 'entity' terms that score in every sibling event, degrading the default-ON entity bridge. |  |
| `src/eval/cachedLlm.ts:109` | medium | workaround | eval-harness | A provider empty/near-empty response is caught (CacheRefusedEmptyError) and silently returned as a normal `cached:false` result; the comment claims it is surfaced to the caller but nothing is surfaced, so the failure is recorded as an ordinary wrong answer and is never re-run on resume. |  |
| `src/eval/corpus.ts:121` | medium | correctness | eval-harness | sampleFromEvents silently returns fewer tokens than requested when filler is exhausted, and the runner labels the cell by targetTokens; the CLI's default sweep (`csm bench run` / `npm run bench:full`) goes to 1B on a 9.0M-token corpus, so 10M/100M/1B cells would be labelled by target with no warning anywhere. |  |
| `src/eval/runner.ts:180` | medium | correctness | eval-harness | When a previously-errored cell is re-run on resume, both the stale error row and the fresh row are kept in `results`, so the in-run summary.json double-counts that cell (one extra correct:false), and disagrees with `bench replay` which dedupes last-wins. |  |
| `src/eval/runner.ts:296` | medium | drift | eval-harness | The catch-block comment says an errored cell is left out of the scores (smaller n) and re-run later, but the code scores it as a wrong answer, and both the in-run early-stop check and reconstructEarlyStops/computeSummaries count error rows as incorrect, so a burst of transient provider errors can permanently early-stop a (system, modelContext) group and depress replayed accuracy. |  |
| `src/eval/runner.ts:331` | medium | correctness | eval-harness | Cells that threw (provider timeout etc.) are pushed into cellScores as correct:false, and reconstructEarlyStops (lines 493-494) counts error rows as incorrect, so a burst of transient errors can permanently early-stop a (system, modelContext) group and make resume skip every larger corpus size; the comment at 297-299 describes the opposite behaviour. |  |
| `tests/askPipeline.test.ts:46` | medium | test-gap | tests | Invariant 7 is only tested as 'some source starts with shard@'; recall.ts copies shard_id/snapshot_id verbatim from the LLM response and every stub provider in the suite echoes input.shardId, so a recall that cites the wrong shard or snapshot is never exercised. |  |
| `tests/coverageAssembler.test.ts:359` | medium | test-gap | tests | resolveCoverageMaxEntries (CSM_COVERAGE_MAX_ENTRIES) is asserted only for unset and a valid '12'; there is no garbage case, and the source would silently default if one were added. |  |
| `tests/embeddingFloor.test.ts:219` | medium | test-gap | tests | resolveEntityBridgeK/Max (lines 218-226) and resolveLexicalBridgeK/Max (lines 264-267) are asserted for defaults and '0' only; no garbage case exists, and all four hand-parse with silent fallbacks. |  |
| `tests/env.test.ts:2` | medium | drift | tests | The header asserts 'one vocabulary for configuration, and no silent defaults', but at least 14 CSM_* numeric/enum readers still hand-parse with silent fallbacks and nothing in the suite scans for them. |  |
| `tests/env.test.ts:2` | medium | test-gap | tests | The header claims 'one vocabulary for configuration, and no silent defaults', but the suite enforces invariant 5 only per-flag for flags someone remembered; no structural test scans for process.env.CSM_* reads that bypass env.ts, and at least 13 such readers remain, two of them with no test at all. |  |
| `tests/env.test.ts:116` | medium | correctness | tests | The AMBIENT clear-list omits CSM_AMB_PREFERENCE_PROFILE and CSM_AMB_FACT_FOLD, yet both are FLAGS rows whose resolvers default their parameter from process.env, so their 'unset' default assertion reads the developer's shell — the tautology the comment at 109-115 says it guards against. |  |
| `tests/env.test.ts:298` | medium | test-gap | tests | The CSM_AMB_ID_REPAIR default test is tautological: it calls envFlag with a fallback the TEST supplies, never the production resolver, so flipping the production default would not fail it. |  |
| `CHANGELOG.md:132` | low | correctness | docs-top-level | The 2026-08-30 'Documentation reset' entry asserts the 98–99.9% turn-span figure as an empirical check with no pointer to the same-day correction (lines 26-33) that found it wrong. |  |
| `CHANGELOG.md:134` | low | drift | docs-top-level | The 'Documentation reset' entry still asserts the retracted 98–99.9% span figure as the empirical check, with no pointer to the correction 100 lines above. |  |
| `CHANGELOG.md:264` | low | drift | docs-top-level | Describes a Node 20 + 22 CI matrix that does not exist; ci.yml runs a single Node 22 job. |  |
| `CHANGELOG.md:279` | low | drift | docs-top-level | Lists `ablate` among shipped bench subcommands; it is a stub that throws. |  |
| `CHANGELOG.md:389` | low | drift | docs-top-level | References a cleanup script that does not exist in scripts/. |  |
| `CLAUDE.md:8` | low | drift | docs-top-level | Says README contains the provider env-var matrix and the Gemma-on-4090 setup; README now delegates both to docs/GEMINI.md and docs/REPRODUCING.md. |  |
| `CLAUDE.md:41` | low | drift | docs-top-level | Names the wrong embedding package. |  |
| `CLAUDE.md:91` | low | drift | core-write-and-contracts | CLAUDE.md tells maintainers the <<MOCK_RESULT>> fences live in src/core/prompts.ts; prompts.ts contains none — they are appended in the stage modules. |  |
| `CLAUDE.md:98` | low | drift | docs-top-level | The MVP stack section says there is no CI, but .github/workflows/ci.yml exists and runs lint/test/build/verify/smoke. |  |
| `CONTRIBUTING.md:25` | low | drift | docs-top-level | Contributor setup says Node 20 is sufficient; package.json engines requires >=22. |  |
| `README.md:86` | low | drift | docs-top-level | '~25% of the token count' is stated as a property of the whole ladder; it holds at 100K–1M but not at 10M. |  |
| `README.md:86` | low | correctness | docs-top-level | 'The internal pipeline is ~25% of the token count' generalises the 100K figure across the whole ladder; at 10M it is ~9.5%. |  |
| `docs/AMB_BEAM_LADDER_2026_06_18.md:150` | low | drift | ops-and-integrations | The only doc that shows how to invoke these scripts gives a ladder command that now fails on the mandatory -Tag and a per-tier command that would resume-write into the completed June 10M run dir. |  |
| `docs/ARCHITECTURE.md:53` | low | drift | core-write-and-contracts | ARCHITECTURE.md and AGENTS.md state that no code path other than the Committer may write a chronicle event, but csm init appends an 'init' chronicle record directly via storage.appendChronicle outside src/core/commit.ts. |  |
| `docs/BENCHMARK_METHODOLOGY.md:41` | low | drift | docs-top-level | Methodology §4 describes four compared systems in present tense; three were deleted from the repo. |  |
| `docs/COST_ACCOUNTING.md:58` | low | drift | docs-top-level | 'Per-baseline status today' lists three baselines that no longer exist in the codebase. |  |
| `docs/EVIDENCE.md:49` | low | drift | docs-top-level | Calls the 100K rerun 'the submitted' rerun and frames the June ladder as 'the public headline', while STATUS frames the headline as flat per-query cost plus certified free-instrument leads. |  |
| `docs/EVIDENCE.md:54` | low | drift | docs-top-level | Two rows describe the flat-cost claim 'across the 100× range' after the magnitude was corrected to 76× for the per-unit haystack. |  |
| `docs/EVIDENCE.md:86` | low | drift | docs-top-level | EVIDENCE says the 500K knowledge_update certification does not attribute the effect to the fact fold ('lever attribution at 500K is inferred'), but README.md:150 and STATUS.md:146 present that certification as the evidence for why the fold is ON. |  |
| `docs/EVIDENCE.md:102` | low | drift | docs-top-level | Cites BENCHMARK_METHODOLOGY.md as the artifact documenting Mem0/HippoRAG as blocked; that file never mentions either system. |  |
| `docs/EVIDENCE.md:125` | low | drift | verifier-and-charts | The evidence map attributes the shared-slice chart's CSM values to the v2-entitybridge summary, but the generator reads the v3-wave1 summary; the ablation-chart row (line 124) likewise omits the v3 summary the chart includes. |  |
| `docs/EVIDENCE.md:127` | low | drift | docs-top-level | 'Scoring is programmatic, not LLM-judged' is listed as a public claim without scope, while the live certified leads and the BEAM ladder are LLM-judged. |  |
| `docs/GEMINI.md:102` | low | drift | docs-top-level | Says the repo has one committed Gemini 3.5 Flash evidence run and quotes v1's 2M result, while EVIDENCE.md names v2-wave1 as the CSM rows of record with a different 2M number. |  |
| `docs/PREFLIGHT_OFFICIAL_LADDER.md:28` | low | drift | docs-top-level | Says env.test.ts pins 'the two decisive defaults'; the same phrase is recorded as corrected to twelve rows in CHANGELOG, and the test does pin twelve. |  |
| `docs/RD_PORTFOLIO_2026_06.md:18` | low | drift | docs-top-level | States the hybrid router 'stays default-off'; it has been default ON since 2026-08-01, and the doc carries no superseded marker although README links it. |  |
| `docs/SCIENTIFIC_RELEASE.md:76` | low | drift | docs-top-level | The release checklist requires sidecar artifacts under services/, a directory deleted from the repo. |  |
| `docs/WRITE_TIME_MEMORY_2026_07.md:195` | low | drift | docs-top-level | The 'Reproduce / verify' section says `npm test` runs 368 tests and line 58-59 says the PR #19 configuration is untouched; both are stale. |  |
| `docs/experiments/EXP-T2-router.md:436` | low | drift | docs-experiments | Two consecutive sections are both numbered '## 7', so the doc's internal '§7' references are ambiguous. |  |
| `docs/experiments/EXP-T3-beam-slice.md:311` | low | drift | docs-experiments | Cost estimates in the T1/T2/T3 protocols use ~5x lower Gemini 3.5 Flash rates than the same-day verified pricing table in EXP-T4. |  |
| `docs/experiments/EXP-T3-beam-slice.md:311` | low | correctness | docs-experiments | Same-day cost estimates in T1/T2/T3 use ~$0.30/M input and ~$2.50/M output for gemini-3.5-flash, while T4's verified pricing table gives $1.50/M and $9.00/M — the T1/T2/T3 dollar figures are ~5x low. |  |
| `docs/experiments/EXP-category-leadership-2026-08.md:30` | low | drift | docs-experiments | The stale-baseline paragraph cites the retracted-pending-re-measurement +0.365 as 'the strongest lever ever measured here' and lists CSM_AMB_PREFERENCE_PROFILE among the things the ladder predates in a way that implies it is now part of the defaults. |  |
| `docs/experiments/EXP-category-leadership-2026-08.md:90` | low | drift | docs-experiments | The 'today's config' n=25 scoreboard gives no run id and its 500K values differ from the labelled g500k-newdefaults-v1 table (preference_following 0.890 vs 0.862; information_extraction 0.675 vs 0.605) while a third control value (0.650) appears in the needle-net section; the prose then mixes them — 'from −0.195 … to −0.015' uses the scoreboard baseline against a needle arm whose paired control was 0.650 (−0.220, as line 218 uses). |  |
| `docs/experiments/EXP-category-leadership-2026-08.md:569` | low | drift | docs-experiments | Two arms the doc says are 'running' (g500k-pfcr-factfold-v1 at line 569; g500k-leanoff-v1 at line 264) have outcomes recorded only in EVIDENCE.md; the experiments record ends on 'running' for the arm that gated the fact-fold default flip. |  |
| `docs/experiments/EXP-headtohead-claude-stack.md:22` | low | drift | docs-experiments | The CSM arm is the v1 artifact at 0.7431, but the doc's conclusion quotes the v2/ladder pair (0.7367 vs 0.7337, '+0.003') as the official comparison it reproduces; against the arm actually used the official gap is +0.009. |  |
| `docs/experiments/EXP-headtohead-claude-stack.md:114` | low | correctness | docs-experiments | The doc uses the v1 rerun artifact (0.7431) as the CSM arm but reports the 'official Gemini comparison' as 0.7367 vs 0.7337 (+0.003), which is the v2 ladder run; against the artifact actually used the official gap is +0.009. |  |
| `docs/experiments/EXP-judge-calibration.md:5` | low | drift | docs-experiments | The free judge was calibrated against the PRE-#20 official judge (gemini-2.5-flash-lite, rubric fraction + Kendall τ-b for event_ordering); every downstream doc cites 'ρ 0.864 / MAE 0.077 vs the official Gemini judge' without noting that STATUS says the official judge, prompt and event_ordering metric changed after upstream PR #20. |  |
| `docs/experiments/EXP-relations-and-hops-2026-08.md:122` | low | drift | docs-experiments | The doc states the warm server never passes preferenceProfile and that this 'must land before the final Gemini ladder', but the server now passes both preferenceProfile and the fact fold; the experiments layer never records the fix. |  |
| `docs/experiments/EXP-router-1m-hybrid.md:75` | low | drift | docs-experiments | Two different Hindsight 1M per-category baselines are both presented as 'Hindsight's 1M numbers' without stating that they come from different query samples (n=12 vs n=15 per category). |  |
| `docs/experiments/EXP-silent-degeneration-class.md:116` | low | drift | docs-experiments | The compounding summary table still carries the pre-correction recall figure ('29% of each turn's text'), which the CORRECTION block replaced with a fixed 480-char cap and a 3.8%-of-shard-text measurement. |  |
| `docs/experiments/EXP-silent-degeneration-class.md:116` | low | correctness | docs-experiments | The compounding summary table's recall row ('29% of each turn's text \| first-N chars') was not updated after the CORRECTION: 29% is the probe tag-noise share from instance 2b, and the corrected instance 3 describes a fixed 480-char cap with 3.8% of shard text surviving. |  |
| `integrations/amb/csm_provider.py:60` | low | dead-code | ops-and-integrations | The 'CSM_REPO_DIR unset' branch can never execute because str(Path('')) is '.', so the docstring's '(required)' is enforced only by an opaque downstream spawn failure. |  |
| `integrations/amb/csm_provider.py:60` | low | correctness | ops-and-integrations | The unset-CSM_REPO_DIR check is dead code: Path('') stringifies to '.', which is truthy and exists, so a missing 'required' variable silently resolves to the AMB checkout and fails later as an opaque 'server exited before becoming ready'. |  |
| `integrations/amb/csm_provider.py:62` | low | dead-code | amb-bridge | `self._model` is assigned and never used (nothing in the file reads it, and it is not sent to the server), and the docstring's 'default gemini-3.5-flash' does not describe the actual default — the server resolves the model via `resolveBridgeModel()` to the active provider's configured model, or undefined. |  |
| `integrations/amb/csm_provider.py:62` | low | drift | amb-bridge | `self._model` is assigned with a hardcoded `gemini-3.5-flash` default and never used; the module docstring (:18) advertises that default, but the server actually resolves the model via resolveBridgeModel (provider default when unset). |  |
| `integrations/amb/csm_provider.py:142` | low | drift | ops-and-integrations | The 'Durable log first (resume/audit)' claim does not hold for the ladder: with --skip-ingested AMB calls prepare(reset=True), which unlinks documents.jsonl on every retry, and the replay path is only reachable via the different --skip-ingestion flag and would post the whole log as one body against the server's 256 MB cap. |  |
| `integrations/amb/csm_provider.py:211` | low | correctness | ops-and-integrations | On a server restart the previous stderr log handle is dropped without being closed. |  |
| `integrations/amb/csm_provider.py:226` | low | correctness | ops-and-integrations | The readiness loop uses a blocking readline(), so the CSM_AMB_SERVER_STARTUP_TIMEOUT_SEC deadline is only checked between lines; a child that stays alive but prints nothing to stdout hangs initialize() forever. |  |
| `integrations/amb/csm_provider.py:316` | low | correctness | ops-and-integrations | csm_internal_total_tokens is written as 0 when the bridge reports no usage, while the component fields are null, so an unmeasured row is indistinguishable from a measured zero in the cost ledger. |  |
| `scripts/amb-csm-retrieve.ts:213` | low | drift | amb-bridge | The `factRegistry` parameter doc says the warm server passes it 'only for aggregation-intent queries', but with the default-ON fold the server passes it on every query (amb-csm-server.ts:576-587). |  |
| `scripts/amb-csm-retrieve.ts:526` | low | drift | tests | Two adjacent comment blocks contradict each other at the exact site invariant 10 governs: one says the profile is prepended as its own document costing a return slot, the next says it is folded into the capsule. |  |
| `scripts/amb-csm-retrieve.ts:578` | low | invariant | amb-bridge | When no capsule exists the fact fold creates a new `csm-fact-registry` document — an append, not a fold — contradicting the module's 'document count unchanged' claim; official telemetry shows the no-capsule case on roughly 1% of rows (100k 6/400, 500k 5/755, 1m 4/729, 10m 0/208 rows with docs_returned == return_k). |  |
| `scripts/amb-csm-retrieve.ts:1902` | low | dead-code | verifier-and-charts | The legacy benchmark-fitted vocabulary tables remain in the retrieval bridge behind CSM_AMB_LEGACY_VOCAB with a comment saying to delete them once the paired A/B is recorded; the A/B has been recorded. |  |
| `scripts/amb-csm-retrieve.ts:2021` | low | dead-code | amb-bridge | `MONTH_INDEX` is defined and never referenced in this file (coverage.ts has its own), and the LEGACY_* vocabulary tables the comment says to 'delete once that [paired A/B] is recorded' are still compiled in although the A/B has been recorded (docs/EVIDENCE.md:181, +0.0037, arm I). |  |
| `scripts/amb-csm-server.ts:199` | low | drift | amb-bridge | The comment says per-scope versioning 'keeps the baseline's WeakMap-keyed router index (pre-warmed at ingest) alive for A', but `CORPUS_CACHE_MAX = 4` evicts oldest corpora by insertion, so after a 35-unit ingest at most 4 pre-warmed corpus objects survive and the router indices of the other 31 are garbage-collected before their queries arrive. |  |
| `scripts/amb-csm-server.ts:200` | low | drift | amb-bridge | The comment says per-scope versioning 'keeps the baseline's WeakMap-keyed router index (pre-warmed at ingest) alive for A', but `CORPUS_CACHE_MAX = 4` means a 35-unit ingest prewarm evicts all but the last four corpora, and with them the WeakMap-keyed router indices — so the router prewarm is effectively discarded for most units and rebuilt at query time. |  |
| `scripts/amb-csm-server.ts:382` | low | correctness | amb-bridge | `factFoldCache` eviction runs only the stale-version pass; the size-bound pass present for `preferenceCache` (:537-540), `observationCache` (:311-314) and `factCache` (:441-444) is missing, so the cache is unbounded while all entries are fresh, despite the docstring promising the 'same contract as getScopedPreferenceProfile'. |  |
| `scripts/amb-csm-server.ts:383` | low | correctness | amb-bridge | `factFoldCache` eviction runs only the stale-version pass; the size-bound pass present for every sibling cache is missing, so the `PREFERENCE_CACHE_MAX` cap is never enforced for fresh entries. |  |
| `scripts/amb-csm-server.ts:504` | low | drift | amb-bridge | The comment says the write-time cache key uses the provider's own model 'never CSM_AMB_MODEL', but `main()` calls `publishBridgeModel(state.defaults.model)` first, which copies CSM_AMB_MODEL into the provider's model slot when that slot is empty, so `resolveProviderModel` then returns CSM_AMB_MODEL's value. |  |
| `scripts/amb-csm-server.ts:506` | low | drift | amb-bridge | The comment says the write-time cache-key model is 'never CSM_AMB_MODEL', but main() publishes CSM_AMB_MODEL into the active provider's model slot before any lazy `resolveProviderModel` call, so when CSM_AMB_MODEL is set and CSM_GEMINI_MODEL/CSM_AGENT_MODEL is not, the write-time builds (and their cache keys) DO use CSM_AMB_MODEL and stop sharing entries with the slice harness. |  |
| `scripts/amb-csm-server.ts:587` | low | correctness | amb-bridge | With the non-default combination CSM_AMB_FACT_MEMORY=1 plus fold ON, an aggregation-intent query builds and pays the legacy registry (:563), the fold registry then replaces its text, but the legacy `buildCost` is still attached and reported in `raw_response.factBuildCost` while never being added to `inputTokens` (the core only adds it in the `factReplace` branch, retrieve.ts:450). |  |
| `scripts/amb-csm-server.ts:588` | low | correctness | amb-bridge | With CSM_AMB_FACT_MEMORY=1 and fold ON (non-default), an aggregation-intent query builds and pays for the legacy registry, discards its text in favour of the folded one, yet still reports `factBuildCost` in raw_response while the core never adds that cost to inputTokens. |  |
| `scripts/amb-csm-server.ts:740` | low | correctness | amb-bridge | `/ingest` pushes documents with no shape validation and no dedupe by `doc.id`, so a repeated ingest of the same unit (a runner retry, or a resume replay after a partial in-RAM ingest) duplicates events: `byId` last-wins but `byShard` and `totalTokens` double. |  |
| `scripts/amb-csm-server.ts:754` | low | correctness | amb-bridge | `/reset` clears `observationInflight`, `factInflight` and `preferenceInflight` but not `factFoldInflight`, an unexplained asymmetry; tests/ambServer.test.ts:144 asserts only `documents.length` and `corpusCache.size`, so nothing pins either behaviour. |  |
| `scripts/amb-csm-server.ts:762` | low | correctness | amb-bridge | `/reset` clears every in-flight map except `factFoldInflight`, an unexplained asymmetry; harmless in practice because entries self-delete on settle (:368/:379) and scope versions are monotonic, but the reset test does not cover it. |  |
| `scripts/free-ladder-report.mjs:83` | low | drift | gate-scripts | The report's `certified` label is granted on \|mean\| > 2.8·se alone — no CI check and no full-n check — so it can disagree with headtohead-arms' verdict and with STATUS's definition of 'certified' on identical data. |  |
| `scripts/free-ladder-report.mjs:115` | low | correctness | gate-scripts | Two denominators are mixed inside one report: token/latency/internals for CSM average over ALL payload rows, while answer-visible tokens average over scored ids and silently drop ids absent from contexts.json. |  |
| `scripts/free-ladder-report.mjs:203` | low | drift | gate-scripts | A stale, tier-specific latency claim ('3.47s at 100K on Gemini, 1.84x faster than Hindsight') is hard-coded into the report generated for EVERY tier, including 500K/1M/10M. |  |
| `scripts/free-ladder-report.mjs:205` | low | drift | gate-scripts | A stale pre-#20 100K latency claim is hard-coded into every tier's generated report section. |  |
| `scripts/headtohead-arms.ts:58` | low | invariant | gate-scripts | `CSM_AGENT_BASE_URL` is read raw from process.env (also answer-arms.ts L35, judge-arms.ts L50, calibrate-judge.ts L79), bypassing src/utils/env.ts, which exports no string/URL reader. |  |
| `scripts/headtohead-arms.ts:80` | low | drift | gate-scripts | The Hindsight arm is loaded from a gitignored local file with no hash check, while the script header and docs describe the artifact as 'sha-verified'. |  |
| `scripts/headtohead-arms.ts:95` | low | drift | gate-scripts | The head-to-head (and calibrate-judge) cache key omits `thinkingCacheTag()` that answer-arms and judge-arms include, contrary to cache.ts's stated single-builder contract. |  |
| `scripts/headtohead-arms.ts:201` | low | correctness | gate-scripts | Pairing is by query_id only and --tier is a label, so a contexts.json from one tier paired with another tier's Hindsight artifact scores silently. |  |
| `scripts/measure-hindsight-10m-span.mjs:63` | low | correctness | gate-scripts | A unit with no denominator entry (or a doc id that is not the unit id) yields NaN silently instead of failing. |  |
| `scripts/run-beam-ladder.ps1:9` | low | drift | ops-and-integrations | All three .ps1 files contain U+2014 em dashes (in comments) and have no BOM, violating the operator's stated keep-.ps1-ASCII rule. |  |
| `scripts/run-beam-ladder.ps1:57` | low | security | ops-and-integrations | The stale-worker sweep force-kills any uv.exe/python.exe whose command line merely contains the substring 'omb', not anchored to the AMB checkout or the `omb run` token. |  |
| `scripts/run-beam-slice.ts:570` | low | correctness | gate-scripts | A single runOne rejection is uncaught inside the worker, so one provider error aborts the whole Promise.all with no per-query failure record while detached workers keep appending. |  |
| `scripts/run-beam-slice.ts:573` | low | correctness | gate-scripts | A single provider rejection in runOne rejects Promise.all with no failure record; run-summary.json is never written while the other workers keep appending. |  |
| `scripts/run-beam-tier.ps1:51` | low | drift | ops-and-integrations | The script publishes the generic CSM_MODEL, which src/providers/LlmProvider.ts documents as the write-side hazard that caused three mis-routed-model incidents; the AMB README recipes do the same. |  |
| `scripts/run-beam-tier.ps1:51` | low | workaround | ops-and-integrations | The script publishes the model id to the generic CSM_MODEL, which the provider layer documents as the root cause of past mis-routed-model incidents and says writers must never target. |  |
| `scripts/run-beam-tier.ps1:96` | low | workaround | ops-and-integrations | The AMB .env override guard only scans for CSM_* lines, but AMB's load_dotenv(override=True) would equally override the billing key and the answer/judge model identity exported by this script. |  |
| `scripts/run-beam-tier.ps1:96` | low | correctness | ops-and-integrations | The override-trap guard only scans the AMB .env for CSM_* lines, but AMB's load_dotenv(override=True) would equally override GEMINI_API_KEY/GOOGLE_API_KEY (billing account) and OMB_ANSWER_*/OMB_JUDGE_* (answer/judge identity) that this script exports. |  |
| `src/cli/index.ts:101` | low | drift | storage-utils-cli | Three different graph counts are stated for the same bench-report artifact set, and the generated report.md tells readers to render specs via the online editor although the repo ships scripts/render-plots.ts for exactly that. |  |
| `src/cli/index.ts:132` | low | drift | storage-utils-cli | cmdInit appends a chronicle row directly through storage.appendChronicle from the CLI, outside src/core/commit.ts, which is not one of the two entry points invariant 2 / AGENTS.md name; the invariant text also omits createShard. |  |
| `src/cli/index.ts:132` | low | test-gap | tests | No structural test enforces invariant 2 (durable writes only via commit.ts); the one direct caller outside commit.ts is cmdInit's chronicle 'init' entry, and a new bypass would not be caught. |  |
| `src/cli/index.ts:155` | low | security | storage-utils-cli | User-supplied `--id` (shard create), `--shard` (remember/commit) and `--snapshot`/`<shardId>` (inspect) are used raw as filesystem path segments (paths.ts:26,34 `join(...)`) with no character validation anywhere; createShard (commit.ts:145) only checks manifest existence. |  |
| `src/cli/index.ts:499` | low | correctness | storage-utils-cli | Two more numeric CLI flags silently default on garbage: --max-tokens 0 or non-numeric becomes 1024, and --limit non-numeric becomes 'all rows'. |  |
| `src/core/ask.ts:107` | low | drift | core-read-path | In-code documentation states coverage mode is default OFF; it has been default ON since 2026-06-10. |  |
| `src/core/ask.ts:160` | low | invariant | core-read-path | On the lexical fallback path and the recall cut, the `SelectionResult` degeneracy report is computed and then discarded; nothing on `AskRunResult`, `QueryRunRecord` or the CLI surfaces it, so the operator is never told when the ranking was arbitrary. |  |
| `src/core/ask.ts:231` | low | invariant | core-read-path | The local probe pre-gate ranks candidates with a bare `.sort(...).slice(...)` on cheap-scorer scores, with no degeneracy report (invariant 4). |  |
| `src/core/ask.ts:655` | low | drift | core-read-path | The `resolveProbeBatch` docblock says 'Default OFF' while the function body defaults ON for every hosted provider. |  |
| `src/core/commit.ts:145` | low | security | core-write-and-contracts | createShard accepts an unvalidated shard id that is joined directly into filesystem paths, so `csm shard create --id ../../x` writes manifest/snapshots outside data/shards. |  |
| `src/core/commit.ts:239` | low | dead-code | core-write-and-contracts | Dead ternary with identical branches; 'update' is functionally identical to 'write' except for the chronicle type, so nothing is ever updated or superseded. |  |
| `src/core/commit.ts:263` | low | correctness | core-write-and-contracts | applyCommitDecision never reads decision.requiresUserConfirmation; a decision with action=write and requires_user_confirmation=true is applied unconditionally. |  |
| `src/core/commit.ts:292` | low | correctness | core-write-and-contracts | The freeze path has no status precondition: freezing an already-frozen or archived shard 'succeeds', rewrites the manifest, and appends a duplicate shard_frozen chronicle record, while dryRunCommit unconditionally reports 'status:active→frozen'. |  |
| `src/core/coverage.ts:6` | low | drift | core-read-path | The module header says the "current mitigation" is the bridge's evidence capsule with "hardcoded security/database/weather/performance term tables", but those tables were removed in audit F9/F10 and now exist only behind `CSM_AMB_LEGACY_VOCAB` (scripts/amb-csm-retrieve.ts:1821-1829, "not a supported configuration"). |  |
| `src/core/digestSelection.ts:120` | low | invariant | core-read-path | `orderCandidates` in `reorderBySalience` mode is a bare `.sort()` on `salienceScore` followed by the token-budget cut, with no degeneracy report; listed in the selection.ts header (line 12) as one of the four sites, but still hand-rolled (default-OFF CSM_SIGNALS_RANKER). |  |
| `src/core/prompts.ts:1` | low | drift | core-write-and-contracts | Header attributes all prompts to spec §10, but the recall prompt's 'Relevance guidance' and 'Citation guidance' blocks (:111-121) do not exist in the spec, and the spec's closing instruction was dropped. |  |
| `src/core/routerEmbed.ts:21` | low | drift | core-read-path | Header comment describes the ask.ts integration as "eventual" and "a ≤5-line merge-window edit", but the integration shipped (ask.ts:156-160 is that exact code) and `routerIndex` is on `AskOptions` (ask.ts:65). |  |
| `src/core/routerEmbed.ts:354` | low | correctness | core-read-path | The unit-centroid dimension check inspects only `units[0]`; a later unit with a different length yields a NaN or truncated dot product that `bestUnitScore` silently ignores or accepts. |  |
| `src/core/routerEmbed.ts:389` | low | correctness | core-read-path | The composite tiebreak key does not implement the documented 'higher lexTotal first' policy for fractional or mixed-width `lexTotal` values, because `String(1e9 - lexTotal)` produces variable-width decimals that `padStart(12)` does not normalise. |  |
| `src/core/routerEmbed.ts:394` | low | correctness | core-read-path | `lastHybridSelection` is a process-global written after an `await` and read back by `ask()` in a later microtask, so concurrent `ask()` calls in one process can read another query's degeneracy report. |  |
| `src/eval/baselines/csm.ts:41` | low | drift | eval-csm-baseline | Comments still reference 'the other baselines' (RAG etc.) that were removed from the repo. |  |
| `src/eval/baselines/csm.ts:439` | low | drift | eval-csm-baseline | resolveShardDescriptors docblock states the flag is 'Default OFF' while the function body three lines later sets fallback: true and tests pin it default-on. |  |
| `src/eval/baselines/csm.ts:464` | low | drift | eval-csm-baseline | resolveRouterHybrid docblock says 'Default OFF on mainline … the default flips after the T3 BEAM-slice recall@k A/B confirms it' while the body sets fallback: true. |  |
| `src/eval/baselines/csm.ts:610` | low | workaround | eval-csm-baseline | organizeMemoryScaled/organizeFactsScaled/organizePreferencesScaled declare `model: string` (required) while organizeMemory (559) and BaselineRunContext (types.ts:27) use `string \| undefined`; every production caller casts `as string` to pass undefined through. |  |
| `src/eval/baselines/csm.ts:610` | low | drift | eval-csm-baseline | organizeMemoryScaled / organizeFactsScaled / organizePreferencesScaled declare `model: string` (required) although the value is legitimately undefined ('provider default') per types.ts:27 and organizeMemory:559, forcing every caller to cast. |  |
| `src/eval/baselines/csm.ts:1171` | low | drift | eval-csm-baseline | latencyMs sums only ask() wall-clock plus the final call; the local MiniLM corpus embed (ensureEmbeddings), cross-encoder rerank and router-index build are excluded, while docs/COST_ACCOUNTING.md:9 says totals must include 'embedding lookups, anything' and :63 says RAG's embedding lookup is excluded — the contract contradicts itself and the code silently picks one side. |  |
| `src/eval/baselines/csm.ts:1204` | low | drift | eval-csm-baseline | retrieveContext comment says the hybrid router is opt-in and the default path is the byte-identical Phase-0 path; the default has been ON since 2026-08-01 (line 482-486). |  |
| `src/eval/baselines/csm.ts:1251` | low | drift | eval-csm-baseline | Comment says coverage timeline is 'CSM_COVERAGE, default off → empty array'; coverage is default ON. |  |
| `src/eval/baselines/csm.ts:1641` | low | dead-code | eval-csm-baseline | `askResult.cost ?? {zeros}` is a dead fallback — ask() types cost as required — and if the type ever loosened it would report a zero-cost pipeline while still passing the sum contract. |  |
| `src/eval/baselines/csm.ts:1675` | low | correctness | eval-csm-baseline | meta emits embedFloorFired/embedFloorCount but not the ids the floor added, although `embedFloorAddedEventIds` is already computed; the union path does emit embedUnionAddedEventIds, so floor injections (default-ON, K=10) cannot be reconstructed from run telemetry. |  |
| `src/eval/baselines/csm.ts:1703` | low | correctness | eval-csm-baseline | `truncated` is true whenever fewer events packed than were in retrievalOrder, which conflates 'id not found in corpus and skipped' with 'dropped for budget'. |  |
| `src/eval/baselines/csm.ts:1726` | low | dead-code | eval-csm-baseline | Stale worktree-merge markers: '[T2 WORKTREE WIRING — merge-window material] … The merge window should lift this into a shared src/eval helper' (and tags at 1293, 1698) describe a pending merge that has long since landed (hybrid router default ON since 2026-08-01). |  |
| `src/eval/baselines/csm.ts:1831` | low | drift | eval-csm-baseline | The NOTE on buildShardsFromCorpus says 'on BEAM every entry below is boilerplate … the lexical leg has no query signal at all', describing the pre-2026-08-01 state; with CSM_SHARD_DESCRIPTORS default ON the entries carry TF-IDF terms. |  |
| `src/eval/baselines/csm.ts:2052` | low | correctness | eval-csm-baseline | collectCitedEventIds extracts the event id with lastIndexOf(':'), so a shard-only source (`shard@snapshot`, allowed by prompts.ts:152) on a shard id containing ':' — virtual-shard ids are `${docId}::c0001` — yields a bogus event id like 'c0001@S001'. |  |
| `src/eval/baselines/csm.ts:2076` | low | drift | eval-csm-baseline | BRIDGE_STOP_WORDS is a second, privately-maintained stop list that has diverged from src/core/coverage.ts:COVERAGE_STOP_WORDS — the same duplicate-extractor divergence the 2026-07 audit recorded as F6 and removed in F9 ('The bridge's private extractContentTerms and its stop list went with it; all five call sites now route through one queryTerms()'). |  |
| `src/eval/cachedLlm.ts:45` | low | dead-code | eval-harness | `cached` is never read by any caller, so a result row whose token counts are char/4 estimates (cache hit) is indistinguishable from one with provider-reported usage. |  |
| `src/eval/cachedLlm.ts:66` | low | correctness | eval-harness | Provider identity is folded into the cache key only when `model` is undefined; with an explicit model string, two providers serving the same tag (e.g. ollama and llama-server `gemma4:31b`) share cache entries and one silently replays the other's responses. |  |
| `src/eval/cachedLlm.ts:83` | low | correctness | eval-harness | On a cache hit, inputTokens/outputTokens are char/4 estimates and the `cached` flag is read by no caller (grep: no `.cached` reads outside this file), so a CellResult from a warm-cache resumed run carries estimated token counts indistinguishable from provider-measured usage. |  |
| `src/eval/corpus.ts:79` | low | drift | eval-harness | Doc comments reference a function and a CLI command that do not exist: `loadCorpusStreaming` (TODO) and `csm bench corpus build`. |  |
| `src/eval/corpus.ts:79` | low | dead-code | eval-harness | TODO marker points to a `loadCorpusStreaming` variant that does not exist anywhere in the repo. |  |
| `src/eval/corpus.ts:129` | low | correctness | eval-harness | sampleFromEvents silently returns a short corpus when filler is exhausted, and the runner labels the cell by targetTokens (runner.ts:268) without persisting Corpus.totalTokens; the PaySwift corpus totals 9,015,731 tokens while CORPUS_SIZE_SWEEP (lines 219-225) includes 10M, 100M and 1B, so any sweep at those points would report a ~9M corpus as 10M/100M/1B. |  |
| `src/eval/runner.ts:408` | low | dead-code | eval-harness | `reserveScaffoldingTokens` is accepted, normalised to 512 and written to config.json, but never passed to any baseline or read anywhere else. |  |
| `src/eval/runner.ts:450` | low | drift | eval-harness | Comment asserts correctOption range validation is already done by the schema; McqQueryZ only checks int().positive(), and the range check (validateMcqQuery) is never invoked by loadQueries. |  |
| `src/providers/GeminiProvider.ts:88` | low | drift | providers | The GeminiCacheStats docblock says the counters are consumed by scripts/measure-gemini-caching.ts; that script (and every other non-test file) never calls getCacheStats() or clearExplicitCaches(). |  |
| `src/providers/GeminiProvider.ts:486` | low | correctness | providers | A non-JSON error body (e.g. an HTML 502/503 page from Google's front end or a proxy) is thrown as `non-JSON response …` without the HTTP status, so isTransientGeminiError cannot see the status and the call fails without any retry even when the status is retryable. |  |
| `src/providers/GeminiProvider.ts:568` | low | invariant | providers | When CSM_GEMINI_CACHE≠off and CSM_GEMINI_USAGE_LOG is set, the read-only query path appends to an arbitrary operator-chosen file; nothing enforces the comment's 'NEVER point it inside a CSM data store', and the mutation-safety test never exercises this provider. |  |
| `src/providers/GeminiProvider.ts:644` | low | invariant | providers | CSM_GEMINI_THINKING / CSM_GEMINI_THINKING_MIN are only validated (envEnum) inside the `startsWith("gemini-3")` gate, so for any other model id — including the alias CLAUDE.md:82 says 'also works' — an invalid value is silently accepted and `disableThinking` is a silent no-op. |  |
| `tests/commit.test.ts:8` | low | test-gap | core-write-and-contracts | The Committer's guards are largely untested: no test for action=update, split/merge dry-run, requiresUserConfirmation, freezing a non-active shard, or appendEventAndSnapshot rejecting a frozen shard. |  |
| `tests/env.test.ts:16` | low | drift | docs-experiments | The header comment says four defaults flipped on 2026-08-01 but names only three. |  |

## unaddressed (97)

| file:line | sev | category | layer | claim | note |
|---|---|---|---|---|---|
| `tests/ambServer.test.ts:319` | high | test-gap | tests | The profile and fact-fold tests assert only that the artifact rides in exactly one document; they never assert document-count parity with the flag-off arm, while the bridge has a live code path that APPENDS a standalone document when no capsule exists — invariant 10 has an append path and no test that would detect it. |  |
| `tests/ambServer.test.ts:319` | high | invariant | tests | The preference-profile server test's own comment admits the profile becomes 'its own document otherwise' and then asserts only carriers.length === 1, so the append branch at scripts/amb-csm-retrieve.ts:558-568 is untested (invariant 10). |  |
| `tests/ambServer.test.ts:424` | high | invariant | tests | The fact-fold server test asserts only that one returned document carries the registry text, never that the fold-ON document count equals the fold-OFF count, so the standalone-document APPEND branch in the bridge is invisible to it (invariant 10). |  |
| `.env.example:40` | medium | drift | providers | The copyable env template documents the hybrid router as 'opt-in' with `CSM_ROUTER_HYBRID=0`, but the code default has been ON since 2026-08-01 and STATUS.md lists it as the certified default. |  |
| `.env.example:70` | medium | drift | providers | `CSM_GEMINI_REASONING_EFFORT` is documented as a live knob (.env.example:70 and docs/GEMINI.md:193, alongside `npm run proxy:start` at GEMINI.md:194) but no code reads it; its only reader was deleted in commit 08e081b. |  |
| `docs/experiments/EXP-recall-breadth-census.md:90` | medium | workaround | docs-experiments | The doc declares a 4x recall-count discrepancy between the official runner and the slice harness 'a direct threat to the validity of the entire free-iteration approach' that 'must be resolved before any slice-based result is used to justify a config change', and no experiments doc records it being resolved, yet every subsequent free-instrument verdict and default flip rests on the slice harness. |  |
| `scripts/build-readme-charts.ts:646` | medium | correctness | verifier-and-charts | The BABILong ablation colour scale declares only two runs while the data carries three, so the 'current pipeline' bars are emitted with no fill and are invisible in the committed SVG; only their value labels float above empty space and the legend omits them. |  |
| `scripts/build-readme-charts.ts:893` | medium | drift | verifier-and-charts | The BEAM 100K chart generator still emits the subtitle 'submitted upstream, pending acceptance'; the committed SVG contains that text and is embedded in a public doc, and re-running `npm run charts:readme` would re-emit it. |  |
| `scripts/category-leaderboard.mjs:155` | medium | drift | gate-scripts | Prints 'Categories CSM LEADS' for any delta > 0.0005 with no MDE/CI/n, and compares CSM's pre-#20 artifacts against Hindsight fetched from unpinned upstream `main` with a parity guard that checks only model NAMES. |  |
| `scripts/patch-amb-csm-provider.ts:59` | medium | drift | amb-bridge | The installer modifies upstream `src/memory_bench/llm/gemini.py` (adds an `OMB_GEMINI_TIMEOUT_MS` client timeout and widens the answer/judge retry guard to timeout/ReadTimeout/RemoteProtocolError), while README.md, STATUS.md and run-beam-tier.ps1 describe the official instrument as the 'unmodified public AMB runner … a 3-file CSM provider and nothing else'. |  |
| `scripts/verify-hindsight-ladder.mjs:36` | medium | test-gap | verifier-and-charts | The script cited as the check for Hindsight's side of the ladder asserts nothing: it prints recomputed values, fetches from the unpinned upstream `main` branch, and swallows fetch/parse errors, so it always exits 0. |  |
| `scripts/verify-published-claims.ts:146` | medium | test-gap | verifier-and-charts | The verifier's 40 EXPECTED rows and four McNemar pairings all pin PaySwift/BABILong/Gemini cells that EVIDENCE.md:56-59 says are 'no longer featured as public headline claims', while the LIVE headline claims (the two certified category leads in STATUS.md:35-36 / README.md:197-198) have zero verifier rows and their run directories are untracked. |  |
| `src/core/schemas.ts:86` | medium | invariant | core-write-and-contracts | recallResultSchema.shard_id / snapshot_id are free strings echoed by the model, and recall.ts copies them into RecallResult without comparing to the snapshot that was actually recalled, so every downstream citation is model-asserted rather than system-asserted. |  |
| `src/eval/rerank.ts:37` | medium | dead-code | eval-harness | CSM_HYBRID_RERANK is a dead flag: `rerankerEnabled()` has no caller in src/ or scripts/ (only tests/env.test.ts:176), the only `rerank()` consumer (src/eval/baselines/csm.ts:1227) is gated by CSM_PROBE_LOCAL_KEEP, the header refers to a removed `hybridRag` baseline, and docs/STATUS.md:149 names CSM_HYBRID_RERANK as the "coverage reranker" flag when the coverage reranker is gated by CSM_AMB_COVERAGE_RERANK (scripts/amb-csm-retrieve.ts:1030-1033). |  |
| `tests/beamLeakageFirewall.test.ts:74` | medium | test-gap | verifier-and-charts | The firewall walks only four retrieval entrypoints, but six other scripts import the retrieval bridge (`scripts/amb-csm-retrieve.js`) and are never walked; a bridge-importing script that imported one of the allowed gold consumers would reach gold transitively and pass every case in the file. |  |
| `tests/factFold.test.ts:5` | medium | drift | tests | The header says the flag 'defaults OFF' while the test body asserts it defaults ON and STATUS.md lists the fold as ON. |  |
| `tests/factFold.test.ts:83` | medium | workaround | tests | The only invariant-10-adjacent assertion is a source-text grep of the bridge for exact expression strings; it pins whitespace and identifier names rather than behaviour, depends on cwd being the repo root, and would pass a semantically wrong change that keeps the strings. |  |
| `tests/measureGeminiCaching.test.ts:310` | medium | drift | tests | A 'KNOWN RED — this is a real defect … CRASHES outright' comment block describes three source gaps that have since been fixed; the test passes and the whole suite is green. |  |
| `tests/routerEmbed.test.ts:168` | medium | test-gap | tests | Invariant 4 ('a component that cannot discriminate must SAY so') is untested at every production call site: the all-zero-score tests assert the degenerate output ordering but never that the caller was told the ranking was arbitrary; `discriminated`/`degenerateReason` are asserted only in tests/selection.test.ts at the primitive level. |  |
| `tests/routerQueryIndependence.test.ts:94` | medium | test-gap | tests | The router degeneracy tests assert the degenerate OUTPUT (alphabetical first-8) but never that the router REPORTS non-discrimination; invariant 4 ('must SAY so') is tested only at the primitive level in selection.test.ts, never at a production call site. |  |
| `tests/routerRecallPayswift.test.ts:31` | medium | drift | tests | This test runs under plain `npm test` with a 300 s timeout and needs a ~80 MB model download on first run, so README's '549 tests, no API keys' and CLAUDE.md's 'runs without API keys' are true but the suite is not offline-clean as those lines imply. |  |
| `docs/experiments/EXP-recall-breadth-census.md:14` | low | correctness | docs-experiments | The 'recall > 1' fractions 37/755 and 46/729 (and EXP-silent-degeneration's 683/729) use the raw official telemetry row count as denominator, but those files contain duplicate rows: 755 and 729 rows collapse to exactly 700 unique (user_id, query_sha256) pairs, matching the 700-query census. |  |
| `docs/experiments/EXP-router-query-independence-bug.md:9` | low | drift | docs-experiments | Code line references in several experiment docs point at line ranges that no longer hold the cited code (the router now delegates to select(); csm.ts and ask.ts have been refactored). |  |
| `integrations/claude-agent/server.mjs:76` | low | invariant | ops-and-integrations | CSM_AGENT_MAX_TURNS and CSM_AGENT_PORT are hand-parsed with Number.parseInt; a garbage value becomes NaN and, for MAX_TURNS, is handed to the SDK unvalidated (the port case fails loudly). The matching read in src/providers/AgentSdkProvider.ts:50 uses a bare `??` for a numeric CSM_* var instead of env.ts. |  |
| `scripts/answer-arms.ts:289` | low | correctness | gate-scripts | Answer-stage failures are counted to stdout only and never persisted, so judge-arms' `excluded` and answer-gate-v2.json understate total pair loss (and answer-gate-v2.json omits runA). |  |
| `scripts/build-readme-charts.ts:6` | low | drift | verifier-and-charts | The header lists seven output SVGs but main() writes eight; EVIDENCE.md repeats the seven-file list. |  |
| `scripts/build-readme-charts.ts:142` | low | correctness | verifier-and-charts | Correct-count labels are derived as `accuracy * 30` with a hardcoded n, although every summary cell carries an `n` field. |  |
| `scripts/build-readme-charts.ts:426` | low | correctness | verifier-and-charts | The citation-F1 y-axis sort array names 'LightRAG (SOTA)' but the datum label is 'LightRAG (2025)' (line 404), so the entry never matches and Vega places LightRAG last — below long-context — breaking the intended descending order in the committed chart. |  |
| `scripts/build-readme-charts.ts:426` | low | dead-code | verifier-and-charts | The citation chart's y-sort array contains 'LightRAG (SOTA)', which never matches the data label 'LightRAG (2025)', and two comments still call LightRAG the SOTA. |  |
| `scripts/build-readme-charts.ts:728` | low | correctness | verifier-and-charts | The official-leaderboard chart assigns the CSM teal (C.csm) to 'ARMT fine-tune', while the adjacent shared-slice chart (line 805) uses the same teal for 'CSM (ours, n=30)'. |  |
| `scripts/build-readme-charts.ts:836` | low | drift | verifier-and-charts | The BEAM 100K panel values are hardcoded literals, contrary to the header's statement that numbers are read from committed artifacts; Hindsight's 6.379s has no machine-readable source in the repo. |  |
| `scripts/calibrate-judge.ts:330` | low | correctness | gate-scripts | Per-category agreement table mixes denominators: official mean over all rows, free mean and MAE over non-excluded rows. |  |
| `scripts/calibrate-judge.ts:395` | low | correctness | gate-scripts | The mechanical PASS BAR is printed for whatever --split was run (including train), so a PASS verdict is not bound to holdout and the 'budget is 2' holdout-peek rule is logged, not enforced. |  |
| `scripts/emit-run-contexts.ts:120` | low | correctness | gate-scripts | A payload query id missing from the slice queries file yields an empty question string instead of a hard failure, and headtohead-arms then asks the CSM arm an empty question. |  |
| `scripts/emit-run-contexts.ts:120` | low | workaround | gate-scripts | A payload id missing from the slice queries file is emitted with an empty question instead of failing, so the CSM arm alone would be asked an empty question by headtohead-arms.ts L243. |  |
| `scripts/judge-arms.ts:206` | low | correctness | gate-scripts | The judge's `excluded` count and answer-gate-v2.json cover only failures in this process; answer-stage failures (answer-arms L289) are stdout-only and never reach the gate artifact, and the artifact does not record runA. |  |
| `scripts/patch-amb-csm-provider.ts:56` | low | workaround | amb-bridge | The gemini.py patch uses exact-string anchors that silently no-op when absent (the `import os` insertion and the `genai.Client()` replacement), yet the script still prints a success message; if only the Client anchor matches, the injected `os.environ` raises NameError at AMB runtime. |  |
| `scripts/score-beam-slice.ts:153` | low | correctness | gate-scripts | Payload rows are not deduplicated by queryId before scoring, so duplicate rows (two processes appending to the same run dir) are scored twice and inflate n and the bootstrap CIs. |  |
| `scripts/score-beam-slice.ts:164` | low | workaround | gate-scripts | A corrupt or unreadable config.json is swallowed and the run is left unlabeled, so a mock run with a broken manifest would print unflagged coverage numbers. |  |
| `scripts/verify-corpus.ts:58` | low | correctness | verifier-and-charts | The script detects duplicate event IDs but only prints the result; it never exits non-zero, so the replication-kit step 'verify-corpus' cannot fail on its own check. |  |
| `scripts/verify-hindsight-ladder.mjs:8` | low | drift | gate-scripts | The only Hindsight-artifact verification script pins to `main` with no sha check, while docs describe the Hindsight arm as 'sha-verified'; no committed script performs that verification. |  |
| `scripts/verify-hindsight-ladder.mjs:8` | low | invariant | gate-scripts | The only Hindsight-artifact verification script in the layer fetches from unpinned `main` and computes no sha, while headtohead-arms.ts L13 and EXP-headtohead-claude-stack.md L26 describe the Hindsight arm as 'sha256-verified against blob-manifest.json' — a file that exists nowhere in the repo and is checked by no script. |  |
| `scripts/verify-published-claims.ts:519` | low | test-gap | verifier-and-charts | assertMcNemar silently intersects the two runs' query IDs and never asserts the paired n, so a shrunken intersection passes as long as aOnly/bOnly/p happen to match. |  |
| `scripts/verify-published-claims.ts:811` | low | drift | verifier-and-charts | The verifier's comments describe claims that are no longer published: 'README/site headline claims 27-9 … and vs LightRAG 6-2' (811-813), 'README/SOTA claims' (line 4), and 'the published 0.743110 / 337-400 … headline' (609-610). |  |
| `src/cli/args.ts:25` | low | correctness | storage-utils-cli | A boolean flag placed before the query positional swallows the positional as its value, so `csm ask --json "q"` fails with the usage error while `csm ask "q" --json` works. |  |
| `src/core/probe.ts:103` | low | correctness | core-read-path | The solo probe passes `relevant_event_ids` to the pipeline unfiltered, while the batched probe filters them to the shard's own event ids; the two paths disagree on the same contract. |  |
| `src/core/probe.ts:393` | low | dead-code | core-read-path | `probeIndexTokenEstimate` is an exported function with no callers anywhere in src/, scripts/ or tests/, and its comment names a consumer that does not use it. |  |
| `src/core/schemas.ts:29` | low | drift | core-write-and-contracts | Comment states Zod's default is 'strict mode' that 'passes through unknown keys'; Zod's default object mode is strip (unknown keys removed), `.strict()` rejects them, `.passthrough()` keeps them. |  |
| `src/core/schemas.ts:43` | low | drift | core-write-and-contracts | Comment describes the providerJson array-salvage branch as returning the FIRST element that parses; the code returns the LAST. |  |
| `src/core/schemas.ts:177` | low | dead-code | core-write-and-contracts | The LLM Committer contract (commitDecisionSchema, CommitDecisionJson, committerPrompt, and the Gemini responseJsonSchema mirror) has zero runtime consumers, and its pieces are mutually inconsistent. |  |
| `src/core/selection.ts:170` | low | dead-code | core-read-path | `packToBudget` has no production consumer (only a 'MANDATED COPY' in scripts/measure-return-strategies.ts:43-49), and its docblock frames it as the fix for "the third bug site: selectEventDigest" — but `selectEventDigest` keeps its own pack loop (digestSelection.ts:85-99) and recall.ts discards the `droppedCount` it does return (lines 139 and 144 read `.text` only). |  |
| `src/eval/baselines/types.ts:34` | low | drift | eval-harness | BaselineRunContext documents maxOutputTokens 'Default 256 leaves slack for verbose models', and the CSM baseline implements that default (csm.ts:1139 `ctx.maxOutputTokens ?? 256`), while runner.ts:399-406 records that 2048 truncated Gemma 31b's chain-of-thought before the ANSWER line and defaults the sweep to 4096. |  |
| `src/eval/beamJudge.ts:31` | low | drift | eval-harness | The header presents (1+tau_b)/2 as 'the official score' for event_ordering without noting that docs/STATUS.md states the post-#20 upstream judge no longer uses Kendall tau-b; the module models the pre-#20 judge only. |  |
| `src/eval/beamJudge.ts:209` | low | drift | eval-harness | literalItemPositions says it prefers the rarest token so common words do not drag the position early, but the code takes the earliest offset of ANY present token. |  |
| `src/eval/beamJudge.ts:210` | low | drift | eval-harness | literalItemPositions' comment says it prefers the rarest token so common words do not drag the position early, but the loop takes the earliest offset of ANY present token — there is no rarity weighting. |  |
| `src/eval/beamJudge.ts:561` | low | drift | eval-harness | The train/holdout split is documented as 'Stratified by category' but splitAssignment is a per-id sha256 parity coin flip with no stratification. |  |
| `src/eval/cache.ts:104` | low | invariant | eval-harness | thinkingCacheTag reads CSM_GEMINI_THINKING and CSM_GEMINI_THINKING_MIN directly from process.env rather than through src/utils/env.ts, contrary to invariant 5 as stated (every CSM_* read goes through env.ts). |  |
| `src/eval/cache.ts:221` | low | dead-code | eval-harness | `_internal = { tmpdir }` is an unused export kept only to justify an otherwise unused `node:os` import. |  |
| `src/eval/corpus/babilong.ts:435` | low | drift | eval-harness | The comment says the gold-answer-driven `inferSupportingLines` fallback is 'rarely hit' because upstream always carries supporting_facts, but every raw BABILong file on disk lacks that field and the fetch script never writes it, so the gold-derived path is the ONLY path in use. |  |
| `src/eval/corpus/beam.ts:64` | low | drift | eval-harness | Comment describes summarization and event_ordering as 'the two categories CSM loses to Hindsight on', which STATUS.md no longer supports (1M losses are instruction_following/knowledge_update/preference_following; 500K knowledge_update is a certified win). |  |
| `src/eval/embed.ts:28` | low | correctness | eval-harness | getPipeline caches a single pipelinePromise for the process regardless of modelName, so a second model name would silently reuse the first model's pipeline while writing vectors under the second model's cache directory (line 40). Latent: every caller today passes EMBED_MODEL_NAME. |  |
| `src/eval/embed.ts:121` | low | dead-code | eval-harness | `makeCachedEmbedder` is documented as the only real EmbedFn implementation injected into the hybrid router, but nothing calls it; the router is wired with an inline lambda instead. |  |
| `src/eval/mcq.ts:381` | low | invariant | eval-harness | The tertiary fallback assigns chosenOption from the first standalone integer in 1..N and sets no parseError or marker, so a ~1/40 guess is indistinguishable in results.jsonl from a confidently parsed answer (Inv 4 spirit: a component that cannot discriminate must say so). tests/mcq.test.ts:66-70 pins the fallback and the absent parseError, so the fallback is deliberate; the missing marker is the defect. |  |
| `src/eval/plotter.ts:210` | low | correctness | eval-harness | computeMultipliers' MultiplierRow doc says maxCorpusAtThreshold is the highest corpus size at which the system 'stayed at/above threshold', but the code takes the max over any passing size even when a smaller size fell below threshold, overstating the headline multiplier on non-monotone curves. |  |
| `src/eval/rerank.ts:2` | low | dead-code | eval-harness | The module header describes a live consumer ('today the L3 cross-encoder path') but rerank() has no callers in src/ or scripts/; the whole file is unreachable except for rerankerEnabled, which only tests import. |  |
| `src/eval/rerank.ts:33` | low | invariant | eval-harness | CSM_RERANKER_MODEL is hand-parsed from process.env (invariant 5); there is no envString helper in src/utils/env.ts so the rule cannot currently be satisfied for free-form strings. |  |
| `src/eval/rerank.ts:96` | low | workaround | eval-harness | On model-load or inference failure rerank() console.warns and returns all-zero scores; its only consumer (src/eval/baselines/csm.ts:1227 -> src/core/ask.ts:226-230) then sorts by those zeros with an index tiebreak, so the local probe gate silently degrades to 'keep the lowest router indices' with no marker in results (Inv 4: cannot discriminate -> must say so). Latent: CSM_PROBE_LOCAL_KEEP defaults to 0 (gate OFF per STATUS.md). |  |
| `src/eval/retrievalScore.ts:100` | low | correctness | eval-harness | stripFacetPrefix's verb list differs from beamJudge.ts:77 rubricItemText's and neither covers verbs present in real rubric data (500k slice: 18 'should list', 9 'avoid', 8 'suggest', 2 'ask', 4 'not'); unstripped verbs are not in FACET_STOP_WORDS so they become facet terms and inflate the term count of the coverage proxy. |  |
| `src/eval/retrievalScore.ts:190` | low | drift | eval-harness | The firewall's own evidence pointer is stale: it says tests/retrievalScore.test.ts cross-validates event ids against the real buildCorpus, but that test does not import buildCorpus; the check lives in tests/beamCorpus.test.ts. |  |
| `src/providers/AgentSdkProvider.ts:50` | low | invariant | providers | CSM_AGENT_PORT (an integer knob) is read raw in the provider and with an unguarded Number.parseInt in the sidecar, so a typo yields a malformed base URL / listen(NaN) instead of an EnvConfigError. |  |
| `src/providers/AgentSdkProvider.ts:63` | low | drift | providers | The agent-sdk path has no output-token cap: the provider drops `maxOutputTokens`, and the sidecar documents a `maxTokens?` body field that its `complete()` never reads. |  |
| `src/providers/AgentSdkProvider.ts:63` | low | correctness | providers | completeJson/completeText forward only system, prompt, model and jsonMode — the LlmProvider contract's required `maxOutputTokens` (and temperature/seed/disableThinking) are silently dropped, and the sidecar's documented `maxTokens?` body field is never read, so this path has no output cap and no temperature control. |  |
| `src/providers/AgentSdkProvider.ts:153` | low | correctness | ops-and-integrations | When the sidecar reports null usage (server.mjs:171 returns null if the result message carries none), the provider silently substitutes a chars/4 estimate with no marker, so a run's token ledger can mix measured and estimated values indistinguishably. |  |
| `src/providers/LlamaServerProvider.ts:30` | low | dead-code | providers | `LLAMA_SERVER_DEFAULT_PROBE_MODEL` and `OLLAMA_DEFAULT_PROBE_MODEL` are exported but read by nothing in the repo, and the docblock describes a CSM_PROBE_MODEL fallback that no code implements. |  |
| `src/providers/OllamaProvider.ts:104` | low | security | providers | OllamaProvider and LlamaServerProvider send a real OPENAI_API_KEY as a Bearer token to the local daemon (or to whatever CSM_OPENAI_BASE_URL points at) whenever the variable is present in the shared .env, even though neither daemon validates the token and a placeholder is all that is needed. |  |
| `src/providers/OpenAIProvider.ts:144` | low | invariant | providers | CSM_OPENAI_REASONING_EFFORT is a fixed-vocabulary knob (minimal\|low\|medium\|high) read raw and forwarded to the API unvalidated, bypassing env.ts. |  |
| `src/providers/OpenAIProvider.ts:190` | low | correctness | providers | The retry loop only retries on HTTP status; any error thrown by fetch itself (ECONNRESET, DNS failure, or the provider's own AbortController firing) propagates immediately with zero retries and no provider-labelled message. |  |
| `src/providers/OpenAIProvider.ts:200` | low | correctness | providers | timeoutMs bounds only the time-to-headers: the abort timer is cleared in `finally` as soon as fetch resolves, so the body phase (SSE loop lines 244-287, or response.json() at 297) runs with no per-call deadline; in streaming mode the "per-call timeout" degrades to undici's inter-chunk bodyTimeout. |  |
| `src/storage/jsonlStorage.ts:108` | low | invariant | storage-utils-cli | The invariant-3 overwrite refusal is check-then-write rather than atomic: fileExists() followed by fs.writeFile with the default 'w' flag leaves a window in which two concurrent writers of the same snapshotId both pass the check and the second silently overwrites. |  |
| `src/storage/paths.ts:12` | low | drift | storage-utils-cli | Several CSM_* variables are read directly from process.env rather than through src/utils/env.ts, contradicting the letter of invariant 5 ('all CSM_* reads go through src/utils/env.ts') without any documented exception. |  |
| `src/utils/ids.ts:29` | low | workaround | storage-utils-cli | nextSnapshotId maps any latestSnapshotId not matching /^S(\d+)$/ to "S001" instead of failing, so a malformed manifest produces a collision with the existing S001 and writeSnapshot (jsonlStorage.ts:108-111) reports the misleading "Refusing to overwrite immutable snapshot …/S001" rather than "malformed latestSnapshotId". |  |
| `src/utils/loadEnv.ts:12` | low | drift | storage-utils-cli | loadLocalEnv resolves the literal '.env' against process.cwd(), while CLAUDE.md says the CLI loads '.env at the repo root'; the installed `csm` bin (or a CSM_HOME pointing elsewhere) run from another directory loads that directory's .env or none. |  |
| `src/utils/loadEnv.ts:16` | low | workaround | storage-utils-cli | The comment says the catch swallows "a malformed .env", but Node's process.loadEnvFile parser does not throw on malformed lines (verified on Node 24.15: a file with `this is not valid ===` loaded the surrounding keys and did not throw); the catch therefore only hides I/O errors (EACCES, EISDIR), after which the CLI continues on shell env and, absent CSM_PROVIDER, on MockProvider with no warning. |  |
| `tests/ambServer.test.ts:167` | low | workaround | tests | Two tests set CSM_AMB_LEGACY_INTENT=1 and delete it at the end of the body with no try/finally or afterEach, so any assertion failure mid-test leaks legacy intent ON into the remaining tests in the file. |  |
| `tests/ambServer.test.ts:167` | low | correctness | tests | Two tests set process.env.CSM_AMB_LEGACY_INTENT = "1" and delete it at the end of the body (lines 208, 247) with no try/finally or afterEach, so an assertion failure mid-test leaks legacy-intent ON into the later retrieval tests in the same file. |  |
| `tests/beamSliceRun.test.ts:38` | low | test-gap | tests | The mock-mode e2e stubs four retrieval knobs to 0, and tests/cost-accounting.test.ts:183-185 does the same, so no offline test exercises the shipped default retrieval configuration (floor K=10, expand K=3, entity K=6). |  |
| `tests/beamSliceRun.test.ts:40` | low | test-gap | tests | The 'mock-mode e2e' stubs CSM_EMBED_FLOOR_K, CSM_SHARD_EXPAND_K, CSM_LEXICAL_BRIDGE_K and CSM_ENTITY_BRIDGE_K to 0 (as does tests/cost-accounting.test.ts:183-185), so no offline test runs the shipped default retrieval configuration end to end. |  |
| `tests/coverageAskWiring.test.ts:22` | low | drift | tests | Header item 1 states coverage is 'OFF (default)', stale since the 2026-06-10 default flip that the same file acknowledges at 188-190. |  |
| `tests/factFold.test.ts:84` | low | workaround | tests | The only invariant-10-adjacent assertion is a source-text grep on scripts/amb-csm-retrieve.ts for exact expressions; it pins the reduced raw-doc CUT gate, not the no-append property, and is brittle to whitespace and cwd. |  |
| `tests/leanReturn.test.ts:51` | low | drift | tests | Three comments say unset 'now resolves to k=16' / 'the default flipped', contradicting the file header (default 0, same-day revert) and the assertion at 177-183. |  |
| `tests/leanReturn.test.ts:80` | low | drift | tests | Inline comments say unset 'now resolves to k=16' (lines 79-80) and 'The default flipped on 2026-08-01' (line 162), contradicting the file header (lines 16-17: shipped default 0, flip reverted same day) and the assertion at lines 177-183 that unset resolves to k: 0. |  |
| `tests/mutationSafety.test.ts:86` | low | test-gap | tests | The SHA-256 read-only guard exercises only src/core/ask.ts with MockProvider and no router index (lexical path); no hash-style test covers the AMB bridge/server retrieve path, which does write to disk during /retrieve. |  |
| `tests/prefixCacheContract.test.ts:185` | low | dead-code | tests | Two type aliases exist solely to silence unused-import diagnostics, and the comment cites eslint, which this repo does not have. |  |
| `tests/routerEmbed.test.ts:176` | low | drift | tests | Comment attributes the alphabetical top-8 to 'Stable sort preserves directory (alphabetical) order', but the router now breaks ties by an explicit id key, not sort stability. |  |
| `tests/routerQueryIndependence.test.ts:4` | low | drift | tests | The header describes selectCandidates as ending in '.filter().sort().slice()', code that no longer exists — router.ts delegates to select() and exports selectCandidatesDetailed. |  |
| `tests/schemaRobustness.test.ts:86` | low | drift | tests | The rationale comment says Zod object schemas are 'strict by default' and then describes strip behaviour; Zod's default is strip, and .strict() is what rejects unknown keys. |  |
| `tests/scorer.test.ts:211` | low | test-gap | tests | Test titled 'preserves order in output relative to input' asserts only the output length, not that adjusted p-values are returned in input order. |  |
